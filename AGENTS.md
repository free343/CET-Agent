# CET-Agent Repository Guide

Last updated: 2026-08-24 (Asia/Shanghai)

This file is the authoritative, continuously maintained project-state document for agents working in `D:\work\english`. Read it completely before modifying code. Update it whenever architecture, completed behavior, verification evidence, known gaps, or development constraints change.

## 1. Project mission

CET-Agent is a local-first PySide6 desktop learning agent for Chinese CET-4/CET-6 students. It records review behavior, schedules deterministic spaced repetition, discovers personal vocabulary-confusion patterns, proactively reminds the user, and uses a small LLM only to explain algorithmically discovered problems.

The central rule is:

> Algorithms discover problems. LLMs explain problems.

The LLM must never control scheduling, queue ordering, database queries, statistics, edit distance, relation scoring, graph construction, clustering, or reminder timing.

## 2. Required architecture

```text
PySide6 UI
    ↓
Application Services
    ↓
Deterministic Domain Algorithms
    ↓
SQLite / LLM / Embedding / Notification Adapters
```

Layer responsibilities:

- `app/ui/`: rendering, input, navigation, and asynchronous presentation only. It must not query SQLAlchemy directly or call a concrete model adapter.
- `app/services/`: application use cases, transaction boundaries, orchestration, structured data extraction, and safe model fallbacks.
- `app/domain/`: deterministic, side-effect-free learning algorithms wherever practical.
- `app/db/`: SQLAlchemy models, repositories, UTC persistence, seed data, and database lifecycle.
- `app/ai/`: replaceable LLM/Embedding interfaces, concrete providers, centralized prompts, and Pydantic output schemas.
- `app/infrastructure/`: desktop notification and other operating-system adapters.
- `scripts/`: repeatable demo or validation utilities; scripts must be safe to run more than once when documented as idempotent.
- `tests/`: algorithm, service, provider-cache, reminder, and UI-regression tests.

Do not add a framework or abstraction unless the current boundary actually needs replaceability. The intended replaceable seams are LLM Provider, Embedding Provider, Repository/data evolution, and operating-system notification adapters.

## 3. Current repository facts

- Workspace: `D:\work\english`.
- The workspace is a local Git repository on branch `main`; no remote is configured. Inspect files before editing and preserve unrelated work.
- The schema-v13 adaptive lexical-fact milestone is tracked as one Git milestone, including the complete 4,611-record formal artifact/provenance, source-backed relation overlay, import/query modules, validators, UI integration, packaging updates, tests, and this state document. Use `git log -1 --oneline` and `git status --short` for the current commit identity and worktree state instead of copying a commit hash into this self-versioned file.
- The phase-2 lexical-card source foundation, form-candidate artifact, and expanded relation-candidate overlay are implemented. The formal lexical-fact JSONL remains unchanged; the v3 relation candidate JSONL is now safely imported into isolated schema-v13 columns and shown as `来源候选 · 待审核`. Candidate targets require a positive ECDICT frequency, compatible WordNet POS, and an ordinary learner-facing Chinese gloss; specialist-only rows such as `homo → [医] 人属` are rejected. It never becomes a `source_validated` fact, never creates a Word/review obligation, and retains its candidate-only status. The source manifest, rights/integrity validator, ECDICT/OEWN/COW readers, deterministic coverage/conflict audit, translation-quality gate, candidate schemas/builders/validators, tests, `docs/LEXICAL_CARD_PHASE2_PLAN.md`, the checked audit snapshot, and both candidate JSONL/provenance pairs are present.
- Python target: 3.11+.
- Runtime stack: PySide6, SQLite, SQLAlchemy 2.x, py-fsrs 6.3.2, Pydantic, httpx, python-dotenv, and Windows-Toasts 1.3.1/WinRT 3.2.1 on Windows.
- Test runner: pytest.
- Main entry point: `main.py`.
- Source-mode runtime database: `data/cet_agent.db` (ignored by source control rules). Frozen Windows builds use `%LOCALAPPDATA%\CET-Agent\data\cet_agent.db`; logs and the local `.env`/template use the same writable application root.
- Live source-database audit on 2026-08-24: schema 14; 4,611 words, LearningStates, acquisition states, learning aids, and 4,198 retained lexical-fact rows; 113 ReviewLogs, 33 PracticeLogs, 39 acquisition attempts, one favorite, zero mastered words or learning-aid feedback rows, and 2,563 source-backed candidate projections after reimporting the final v3 quality-gated overlay. The v14 serialized cleanup removed 413 legacy rows that had neither formal nor candidate content and retained 328 formal-empty rows because they carry candidate relations. `PRAGMA integrity_check` returned `ok` and `PRAGMA foreign_key_check` returned no violations. Of the retained rows, 3,870 have a non-empty formal form/relation projection and 2,563 have a candidate relation projection.
- Ollama model storage observed on this machine: `D:\model`.
- Vocabulary: 13 curated demo words in `data/sample_words.csv` plus 4,598 validated open-data words in `data/cet_vocabulary_open.csv` (3,320 CET4 and 1,278 CET6); the two files do not overlap.
- Vocabulary/CI cleanup: downloaded ECDICT and FreeDict source archives, the deterministic rebuild copy, and the clean lock-validation virtual environment were removed from the system temp directory after verification. They are reproducible from committed URLs, hashes, lock files, and scripts; no agent-created vocabulary download partials remain. Ignored local validation state under `build/` currently includes the historical packaged-smoke runtime plus the current `packaged-smoke-v14-runtime` and `installer-smoke-v14-runtime` isolated runtimes, four `acquisition-smoke*.db` databases, six lexical-phase/test databases, and the completed DeepSeek generation checkpoints/report under `build/word_learning_aids/`. These are reproducible validation/checkpoint outputs, not source data; do not confuse them with the formal artifacts under `data/`.
- The current ignored `build/lexical_sources/` cache contains the hash-verified exact ECDICT CSV (65,933,428 bytes), Open English WordNet 2025 XML gzip (11,363,503 bytes), and Chinese Open Wordnet 2.0 archive (1,705,740 bytes), plus an extracted COW inspection copy and the generated audit. No `.part` download remains. The application never reads this cache or downloads a dictionary at startup.
- Logs: `logs/cet-agent.log`, rotating and never intended to contain secrets.
- Detailed Phase 1–9 audit snapshot: `docs/handoff/CET_AGENT_HANDOFF.md`. It is historical context; this `AGENTS.md` is authoritative when the two differ.
- Windows installer compiler: Inno Setup 6.7.3 at `C:\Users\Admin\AppData\Local\Programs\Inno Setup 6\ISCC.exe`; its downloaded installer had a valid Pyrsys B.V. Authenticode signature. The compiler remains installed for repeatable local builds, while its downloaded setup cache and external installer-validation runtimes were deleted; the ignored packaged-smoke output retained in `build/` is documented above.

Local Ollama:

- Executable: `C:\Users\Admin\AppData\Local\Programs\Ollama\ollama.exe`
- API: `http://127.0.0.1:11434`
- Version observed this session: 0.32.13
- Chat model: `qwen2.5:3b`, ID `357c53fb659c`, size 1.9 GB.
- Embedding model: `nomic-embed-text:latest`, ID `0a109f422b47`, size 274 MB.
- Current state: both manifests are installed and both models pass the repository's live Provider/Service validation.
- Operational cleanup: the two agent-created temporary Range files used to complete the throttled download were deleted after both model manifests were verified. No download partials remain.

Optional advanced chat on this workstation:

- The ignored local `.env` explicitly enables the independent OpenAI-compatible advanced Provider at `https://api.deepseek.com` with model `deepseek-v4-flash`; the committed `.env.example` remains disabled by default.
- `ADVANCED_LLM_API_KEY` is intentionally blank. For the exact official `api.deepseek.com` host only, configuration safely reuses the existing `DEEPSEEK_API_KEY`; no other endpoint inherits that credential, and no key value is rendered or logged.
- A live contextual lexical-expansion request completed without degradation and returned common `main` alternatives including `primary`, `principal`, `chief`, `major`, `leading`, and `key` with usage boundaries.

## 4. Implemented architecture and features

### Bootstrap and persistence

- `app/bootstrap.py` configures logging, upgrades the database schema, validates and idempotently imports both bundled vocabulary files plus the complete lexical-fact artifact, creates missing LearningState and WordAcquisitionState rows, and activates the selected study level inside one serialized startup transaction. The bounded daily release setting is applied only to untouched words during that level's first activation; later starts never rebase reviewed or already-activated cards. If any initialization step fails, it disposes the newly created Database/Engine before propagating the failure.
- `app/db/migrations.py` owns the explicit sequential schema registry, currently version 14. A single-row `schema_version` table tracks the current version; upgrades take a SQLite write reservation, update schema plus version in one transaction, adopt pre-versioning MVP databases without data loss, and reject databases newer than the application. Schema v3 adds persistent per-level activation state; schema v4 adds per-instance active-review leases; schema v5 removes synthetic `demo_confusion` increments from FSRS aggregates while preserving the ReviewLog evidence used by the demo graph; schema v6 adds nullable pre-review FSRS snapshots for safe one-step undo without pretending legacy logs are reconstructible; schema v7 adds the personal favorite-word table; schema v8 adds the one-to-one `word_learning_aids` table for validated AI-generated example/collocation/word-family content; schema v9 adds one local, latest-report row per learning aid without modifying the generated artifact; schema v10 adds independent optional-practice history that cannot represent an FSRS scheduling event; schema v11 adds persistent pre-FSRS acquisition state/attempt evidence and reversible mastered-word exclusion; schema v12 adds the one-to-one `word_lexical_facts` table for source-validated typed paradigms and grouped lexical relations; schema v13 adds isolated source-backed relation-candidate columns; schema v14 removes only legacy lexical-fact placeholders that have neither formal nor candidate content.
- `app/config.py` loads the runtime `.env`, resolves relative SQLite paths from the writable runtime root, and owns all model, graph, reminder, logging, and bounded study-workload configuration.
- `app/paths.py` separates read-only bundled resources from writable runtime state. Source execution keeps both at the repository root; a frozen executable reads vocabulary/config templates from `_MEIPASS` and writes database/log/config state under the user's local application-data directory.
- Malformed environment values and unsafe model/graph/reminder/workload configuration are rejected at startup: model endpoints must be absolute HTTP(S) URLs, thresholds are bounded, candidate count stays within 1–100, relation weights must be finite/non-negative and sum to 1, reminder windows/cooldowns must be valid, and daily release/group/extra-new-word counts stay within their bounded ranges.
- `app/db/models.py` defines Word, LearningState, ReviewLog, PracticeLog, WordAcquisitionState, AcquisitionAttempt, FavoriteWord, MasteredWord, WordLearningAid, WordLearningAidFeedback, WordLexicalFact, ConfusionEdge, AIAnalysis, EmbeddingCache, StudyLevelActivation, ReminderRuntimeState, and ReminderReviewLease.
- UTC-aware values are converted through a custom SQLAlchemy type so stored SQLite timestamps are consistent and loaded values regain UTC awareness.
- SQLite review writes acquire `BEGIN IMMEDIATE` before reading LearningState because SQLite ignores `SELECT ... FOR UPDATE`; competing application windows therefore cannot both overwrite the same prior state.
- SQLite connections install a 15-second busy timeout and perform a bounded locked/busy retry while first negotiating WAL mode, so simultaneous first launches can converge on one fresh database instead of failing before the serialized migration transaction begins.
- `app/db/seed.py` validates an entire CSV before mutation, including its required columns, unique single-word English headwords, CET4/CET6 levels, nonempty meanings, and bounded numeric fields. Word seeding is idempotent, supports a per-row initial release delay, and applies corrected bundled metadata to existing words without replacing their LearningState or review history.
- `app/db/acquisition_seed.py` idempotently fills missing acquisition rows after vocabulary/LearningState seeding: any real ReviewLog or positive review count adopts proficiency 3, while untouched and `demo_confusion`-only words adopt 0; existing acquisition rows are never overwritten, and the serialized bootstrap transaction makes concurrent starts converge.
- `app/db/study_level_activation.py` persistently activates each selected CET level once. A never-used level rebases only untouched open-vocabulary cards from its activation time, using the validated daily release limit in bootstrap; curated words, reviewed cards, FSRS state, and ReviewLog history are never moved. Existing databases with open-word review history adopt the earliest such review as activation without rescheduling.
- `scripts/build_open_vocabulary.py` reproducibly builds the redistributable CET artifact from hash-pinned ECDICT and FreeDict sources. Only words with an ECDICT CET tag/phonetic/Chinese meaning and an independent FreeDict bilingual/pronunciation entry survive. The generated CSV hash, source hashes, versions, licenses, transformation, and counts are committed alongside the artifact.

### Review and learning loop

- `app/domain/fsrs_scheduler.py` is a deterministic adapter over the official MIT-licensed `py-fsrs` 6.3.2 implementation of FSRS-6. It uses 90% desired retention, one ten-minute learning step, one ten-minute relearning step, a 36,500-day maximum, and no interval fuzzing.
- LearningState persists FSRS Learning/Review/Relearning state and the active step. Schema migration 2 maps untouched legacy cards to Learning step 0 and reviewed legacy cards to Review without discarding their difficulty, stability, or history.
- `app/services/review_service.py` exposes disjoint acquisition-incomplete and learned-due queues in addition to the compatibility union, excludes mastered words, and atomically updates LearningState plus ReviewLog only for formal learning/review ratings. Its validated extra-study default is configurable per application instance while explicit callers remain bounded. `app/services/review_item_view.py` owns the bounded read-only card projection shared by acquisition, formal, and optional practice routes; it also provides deterministic cloze projections for acquisition level 1.
- `STUDY_LEVEL` is validated as CET4 or CET6. Review queues, due counts, reminder inputs, and every dashboard statistic are restricted to the selected level.
- The open wordbank stages each level independently from that level's first activation at no more than 20 newly due words per day; the 13 curated confusion examples remain immediately available and override any open-data duplicate.
- Review timestamps must advance monotonically for each word, preventing delayed or duplicate submissions from moving a learning state backwards.
- Every new ReviewLog stores the exact pre-review due time, prior review time, FSRS phase, and FSRS step in addition to the existing difficulty/stability fields. `ReviewService.undo_review` may delete only that card's latest review, restores the entire prior schedule plus counters in one serialized transaction, and rejects legacy/no-snapshot or superseded reviews without mutation.
- Formal due ordering prioritizes longest overdue, then higher lapse count, then higher error count. The “学习新词” route accepts released cards whose effective acquisition proficiency is below 3; the “到期复习” route accepts only proficiency-3 cards whose formal due time has arrived, including a newly graduated card exactly 24 hours after acquisition completion.
- Review batches default to 30; completing one batch loads another when more words are already due.
- `app/domain/meaning_quiz.py` deterministically selects one correct Chinese meaning plus three unique local-vocabulary distractors. It prefers compatible part-of-speech evidence and nearby frequency, never calls an LLM, and returns no quiz when four reliable choices are unavailable.
- `ReviewService` attaches stable meaning choices to due cards and records exact choice outcome/hint question types through the existing ReviewLog boundary. A wrong choice exposes only Again; active recall remains available as a fallback.
- After the currently released new-word work is complete, the learner may explicitly unlock five untouched future cards for the selected CET level. The serialized transaction refuses to skip an already-due untouched card, never moves a reviewed card or another level, and persists the new due times across restart. Learned-due work remains visible in its separate route and does not silently reorder the new-word queue.
- `app/services/practice_service.py` selects only previously learned, non-mastered cards for four normal deterministic scopes: yesterday's real reviews in the local calendar, most recently learned, aggregate wrong cards, or learned favorites. It also accepts an explicitly supplied, bounded word-ID list for confusion-cluster practice; the custom scope never falls through to a normal query. All routes page with explicit completed-word exclusion and write a bounded `PracticeLog`; they never call `schedule_review`, update LearningState/ReviewLog, change due time or counters, feed the confusion graph, or change reminder state.
- `app/services/acquisition_service.py` owns serialized acquisition attempts and deterministic level transitions. It persists `AcquisitionAttempt`, graduates at level 3 with a precise 24-hour first formal due time, and returns a refreshed card projection without synthesizing FSRS evidence. `app/services/mastery_service.py` owns idempotent marker writes and bounded recovery-list reads while preserving every other word state.
- `app/services/learning_service.py` derives dashboard metrics from ReviewLog rather than trusting only aggregate LearningState values.
- Dashboard statistics ignore future-dated ReviewLog rows so clock/import anomalies cannot inflate completed counts, accuracy, streaks, or wrong-word rankings. The dashboard separately reports untouched new and learned-due counts; non-demo reviews more than five minutes ahead of the current clock produce a visible clock/timezone warning with the latest timestamp, without automatic history repair.
- `app/ui/acquisition_page.py` owns the persistent three-stage round-robin new-word workflow: level 0 Chinese-meaning choice, level 1 translated cloze English choice, and level 2 spelling/direct confirmation. It never creates formal ReviewLogs, keeps unfinished cards at the current group's tail, reloads the next projection after every persisted attempt, and schedules the next group/voluntary release only after the current group is exhausted. Its completion summary is a bounded UI projection of attempts, mistakes by stage, newly graduated words, and the earliest first-review time; it does not alter scheduling. `app/ui/review_page.py` remains the formal-review/free-practice workspace. Both cards expose asynchronous favorite and “完全掌握” actions beside each other; mastered cards disappear only after the marker transaction succeeds. “已掌握单词” is a separate asynchronous recovery page. Formal review retains recognition/active recall followed by Again/Hard/Good/Easy; free practice offers a scope selector and only “没想起/想起来”, persists a PracticeLog, excludes completed words when paging, and has no undo because no FSRS mutation occurred. Review/practice completion summaries expose bounded recall outcomes and, for formal reviews, the earliest next due time. Shared immutable session modes/issue choices now live in `app/ui/study_session_config.py`, while pure completion formatting lives in `app/ui/session_summary.py`; neither module queries the database or owns workers. All study pages keep cumulative progress, bounded learning-aid display, safe answer visibility, assistant-worker tracking, and deferred-close participation.
- `app/services/wordbook_service.py` owns idempotent favorite writes and bounded cross-level wordbook reads. `favorite_words.word_id` is both the primary key and a cascading Word foreign key, so a word can appear only once and the collection survives restarts without affecting LearningState or scheduling.
- `app/services/learning_aid_feedback_service.py` validates a bounded issue enum and idempotently creates or updates the user's latest local quality report under a serialized transaction. Feedback requires an existing validated aid, cascades with that aid, and never changes the source JSONL, FSRS state, review history, statistics, or model prompts.
- `app/services/lexical_fact_view.py` parses only the imported, validated `word_lexical_facts` JSON columns and merges them with existing generated aids into an ordered, bounded projection. It emits only non-empty `词形 / 搭配 / 近反义 / 派生词` sections, formats relation entries as `近义/反义：目标词 词性 翻译` with compact labels such as `adj.`/`adv.`/`n.`/`v.`, without repeating the current headword's part of speech or sense, keeps verified and AI-unreviewed trust per section, and never lets the UI parse raw JSON. Missing artifact rows remain a diagnostic `missing` state rather than a learner-facing empty heading; schema-v14 may omit rows that are empty in both formal and candidate layers.
- `app/ui/wordbook_page.py` lists the personal collection with word, phonetic, CET level, meaning, and any stored example; it supports cancellation of a selected favorite plus double-click/explicit read-only detail actions. Reads and writes are page-owned background tasks, failed reloads clear stale content, and the main-window close coordinator tracks the page worker.
- The main window starts at 1280×760 with a 960×620 minimum. Each acquisition/formal-review/free-practice splitter gives the contextual assistant an initial 400-pixel share and a 340-pixel minimum, while leaving the learning pane as the stretch owner. The main sidebar uses an explicit deep-indigo background, near-white navigation text, and high-contrast hover/selected states. The favorite action, learning-aid panel, wordbook list, and local-answer upgrade action also have explicit theme selectors instead of relying on platform defaults.
- The sidebar exposes separate “词汇查找”, “学习新词”, “到期复习”, “自由复习”, “收藏单词”, and “已掌握单词” entries. Entering any study workspace dismisses a visible reminder banner and publishes the shared active-study lease; the close coordinator tracks acquisition/formal/practice page workers, the vocabulary lookup worker, the mastered recovery worker, and all embedded assistant workers.
- `app/ui/dashboard_page.py` shows new count, learned-due count, completed today, seven-day accuracy, streak, frequent wrong words, and any future-clock warning. Statistics load off the GUI thread; refresh requests arriving during a load are coalesced into one follow-up refresh. The new/due metric cards expose explicit route actions when the main window is available, and each displayed high-frequency wrong word gets a bounded read-only detail action when the shared controller is available.
- `app/services/word_detail_service.py` provides a bounded exact/prefix lookup over the local Word bank; `app/ui/vocabulary_page.py` owns asynchronous search, explicit/double-click detail actions, stale-result clearing, and empty-query protection without creating study obligations. Favorites, mastered words, dashboard wrong words, and each selected analysis-cluster word reuse the same `LinkedWordReference`/`WordDetailController` boundary.

### Confusion analysis

- `app/domain/similarity.py` implements Levenshtein distance, bounded spelling similarity, clipped 0–1 cosine similarity, one-to-one co-error matching, and temporal exponential decay. Nearest temporal matches use sorted binary search instead of a quadratic history scan.
- `app/services/analysis_service.py` selects words with at least two errors in the configured recent window, limits candidates to 100, obtains optional embeddings, scores candidate pairs, replaces graph edges, and exposes clusters.
- The default relation formula is `0.30 semantic + 0.25 spelling + 0.30 co-error + 0.15 temporal`, threshold `0.65`.
- Candidate selection and cluster error counts use the same closed 30-day window; future-dated ReviewLog rows are excluded.
- Relation labels are inferred from normalized weighted contributions, so custom weights cannot produce a label that contradicts the score formula.
- `app/domain/clustering.py` uses connected components; clusters above eight words pass only their highest-weight core words to the LLM.
- The Analysis list defines an explicit readable selected-item style; selection no longer renders dark/white text invisibly against the platform theme.
- The analysis page exposes “练习此词簇” and one read-only detail button per displayed core word. The main window hands bounded core IDs to the free-practice page, which keeps the cluster label visible and offers an explicit exit back to the normal practice scopes; the custom route remains non-scheduling and uses the `CONFUSION_CLUSTER` `PracticeLog` scope.
- `scripts/create_demo_data.py` idempotently creates correlated ReviewLog errors for three groups without mutating LearningState/FSRS fields. Latest deterministic demo result: 7 candidates, 5 edges, 3 clusters (`adapt/adopt/adept`, `economic/economical`, `complement/compliment`).
- `scripts/benchmark_confusion_graph.py` creates an ephemeral dense graph with 100 candidates, 100 synchronized errors per candidate, and 4,950 persisted edges; it asserts the exact result, runs three timed rebuilds, and fails if the median exceeds a configurable five-second budget. It never touches the runtime database.

### LLM and Embedding integration

- `app/ai/llm_provider.py` is the LLM abstraction.
- `app/ai/ollama_provider.py` uses Ollama `/api/chat`.
- `app/ai/openai_compatible_provider.py` supports OpenAI-compatible chat and normalizes `/v1` without duplication.
- `app/ai/embedding_provider.py` uses Ollama `/api/embed` behind an independent Embedding Provider and SQLite cache.
- Chat and Embedding adapters reject non-object JSON responses through their project-defined unavailable exceptions, preserving service-level degradation instead of leaking raw `AttributeError` failures.
- Ollama chat and embedding calls set `trust_env=False`, preventing Windows/HTTP proxy settings from capturing localhost traffic.
- The Embedding adapter allows a 60-second cold start; the observed first load was 20.159 seconds, so the old 20-second limit was too brittle.
- Chat and embeddings have separate provider, base URL, and model settings.
- Embedding cache identity includes provider type, base URL, and model.
- Invalid or non-finite cached vectors are deleted and regenerated. Model calls occur outside SQLite transactions so an Embedding cold start cannot hold a database transaction open.
- Embedding cache persistence uses SQLite conflict-update semantics, so simultaneous workers converge on one valid row instead of raising a unique-key error.
- `app/ai/prompts.py` is the only location for model prompts.
- `app/ai/schemas.py` strictly validates structured cluster analysis with Pydantic, forbids extra fields, caps every text field, permits at most eight word explanations and six bounded exercise options, and rejects raw structured responses above 32,000 characters.
- `app/services/ai_service.py` retries invalid structured JSON once, then returns a safe degraded result; normal chat also degrades without crashing.
- Local and advanced chat Providers have independent provider/model/base-URL/API-key settings. `create_advanced_llm_provider` returns `None` when disabled and constructs an Ollama or OpenAI-compatible adapter when explicitly configured. The official DeepSeek endpoint may reuse `DEEPSEEK_API_KEY` only after its exact host and explicit advanced Provider configuration match; generic compatible endpoints must supply their own advanced key.
- The scrollable Settings page shows advanced-channel state, Provider, model, endpoint, hidden credential status, the actual runtime `.env` path, and a copyable current DeepSeek template. It never displays a key value and explains that restart is required.
- API-key fields are excluded from the Settings representation. Keys remain local to `.env` and are never rendered by the settings page or logged.
- Structured cluster output must explain every algorithm-selected input word exactly once; schema-valid output containing missing, duplicate, or unrelated words is rejected and retried.
- AI cache identity includes prompt version, provider type, model, base URL, cluster words, relation type, and major statistics.
- If another window stores the same AI analysis after the initial cache read, the losing writer reloads the winning validated row and returns it as a cache hit.
- `app/domain/query_routing.py` owns an injectable deterministic policy that normalizes input and returns an explainable `LOCAL`, `CONFIRM_ADVANCED`, or `REFUSE` decision with bounded confidence. English markers use word boundaries, and an off-topic marker is overridden only by explicit metalinguistic intent such as asking for a meaning or translation. Chinese distinction intent includes `区别`, `辨析`, and `混淆`, so the embedded distinction action cannot be rejected before reaching its Provider. Long or complex language tasks and relation expansion without a local fact request explicit advanced confirmation; the ChatPanel preflights the formal/candidate lexical index and sends known relation facts locally before that confirmation. An LLM never selects itself.
- The local assistant rejects empty, general-chat, real-time, and professional out-of-scope requests without calling a model. The Chat UI consumes the structured route instead of comparing a magic confidence threshold.
- Questions above 4,000 normalized characters are refused before any Provider call. Providers request at most 2,048 output tokens, ordinary answers are bounded to 4,000 characters, model names to 200 characters, and the Chat/Analysis text documents retain only 200/300 blocks respectively.
- Chat keeps at most the four most recent successful in-session exchanges and applies a separate 6,000-character history budget before prompt construction. Failed, degraded, refused, and incomplete exchanges are never retained; history is intentionally memory-only and disappears when the application closes. A contextual panel additionally treats every explicitly displayed card as a generation boundary: the formal-review and acquisition pages notify it immediately, clearing visible text plus history even before another question is sent. Same-card follow-ups retain history.
- `ChatPanel` is reusable as both the full assistant page and a collapsible right-side study assistant. Every instance exposes an always-visible `自动选择（推荐） / 本地 / 高级` selector: auto preserves deterministic confirmation, local forces the local path, and advanced explicitly dispatches every in-scope question to the configured advanced Provider while warning about network/API cost. The unavailable advanced item remains visible but disabled with a Settings-page direction. After a successful real local-Provider answer, a configured panel exposes `对内容不满意？试试高级模型`; one click repeats the captured question with the same card snapshot and the exact pre-local history, restores that history before storing the replacement, and never feeds the rejected local answer to the advanced Provider. The offer is absent for deterministic/scope/degraded/failed/advanced results and is invalidated by a new question or card generation. The compact panel captures a labeled snapshot of only the current word at send time, offers three quick questions, and passes at most 2,500 context characters through the centralized Prompt layer. The snapshot includes the displayed meaning/example plus eligible verified lexical-fact sections and generated collocations/word-family entries; its input focus is outside review-shortcut scope. A per-card generation token suppresses late answers/failures from a previous card, and pending advanced-routing choices are cancelled on transition. Missing/answer-hidden acquisition context produces a local explanation without routing or Provider calls. The full assistant keeps its normal cross-question conversation.
- Chat transcript messages retain textual “你”/“CET-Agent” labels and use explicit accessible palettes: indigo for the user, slate for normal assistant output, amber for transient status, and red for degraded/failure output. Questions, model output, labels, and errors are HTML-escaped before rich-text rendering. The UI identifies only local-model, advanced-model, degraded, or deterministic-rule ownership; internal answer/routing confidence remains available to deterministic orchestration but is never rendered in Chat or Analysis output. Deterministic quick-memory output is labeled “本地规则 · 无模型调用”.
- Contextual help uses explicit task boundaries. Example questions request sentence meaning, in-sentence usage, and a substitution exercise. The embedded distinction action requests current-word usage boundaries and receives a dedicated three-part prompt; it may explain only supplied collocations/family data and cannot invent comparison words or claim unsupported personal confusion. For a normal contextual memory question, `AIService` short-circuits before every local Provider call and `app/domain/study_help.py` builds a deterministic quick-memory card from the supplied word, meaning, validated example, collocations, and word-family entries: core meaning, at most two collocation anchors, complete target-form cloze masking, at most two family links, and a ten-second meaning-to-spelling prompt. A missing example receives an explicit spelling-recall fallback. Explicit advanced memory requests remain grounded to the card. Explicit advanced lexical-expansion requests may add 3–6 common candidates from general English knowledge, but the Service deterministically prefixes an unreviewed-model-content label, the Prompt requires per-candidate meaning/usage/example and interchangeability limits, and the result can never masquerade as card content or learning evidence.

### Generated word learning aids

- `app/ai/schemas.py` adds strict bounded schemas for collocations, word-family members, the per-item model output, and the final `WordLearningAidRecord` artifact contract (`schema_version=1`, `source_kind` curated/open, `example_origin` curated/ai_generated, `content_status=ai_generated_unreviewed`, `prompt_version=word-learning-aids-v1`).
- `app/ai/prompts.py` owns the centralized `word-learning-aids-v1` batch prompt; it requires the model to return exactly the input word multiset, makes the 6–18-word example bound mandatory only for AI-generated open entries, requires examples/collocations to contain the headword or a regular grammatical form, and forbids invented etymology, roots, historical origins, homophones, and unverifiable morphology.
- `app/ai/learning_aid_validation.py` is the shared deterministic trust boundary for model records and provenance. It validates source identity, open-example word count, standalone-boundary headword forms (including common regular and configured irregular forms), collocation headword forms, duplicate/obvious-inflection rules, complete order/count, generator-model consistency, artifact/source SHA-256 values, statistics, validation state, and timezone-aware completion metadata. Generation applies a deterministic sanitizer that drops duplicate/self/obvious-inflection family noise and invalid collocations when at least two valid phrases remain.
- `scripts/generate_word_learning_aids.py` is a resumable DeepSeek batch generator. It reads only the two source CSVs (never the database), loads `DEEPSEEK_*` only from the project `.env`/process environment without overriding explicit process values, defaults to `deepseek-v4-flash`, uses DeepSeek JSON Output with thinking disabled and an offline-only configurable 8,192-token budget, and treats `finish_reason=length` as a retryable truncation. It requests batches (`--batch-size`, default 20), validates every returned word, retries structural/transport failures with exponential backoff, feeds concrete validator errors into validation retries, isolates failing words, and checkpoints validated records keyed by word under the ignored `build/word_learning_aids/`. Resume rejects corrupt, stale-source, or mixed-model checkpoint records. Promotion occurs only after complete validation and writes the formal JSONL, provenance, and deterministic sampled report.
- `scripts/validate_word_learning_aids.py` is an independent, offline, repeatable strict validator. `--require-complete` now requires the exact 4,611-word set plus matching provenance and exits non-zero for any content, identity, statistics, or hash mismatch.
- `app/db/learning_aid_seed.py` reads the whole JSONL before any write, and `app/bootstrap.py` supplies the validated bundled CSV rows plus provenance/source paths so startup independently repeats the complete source/content/hash gate before opening its serialized import transaction. The importer matches existing `Word` rows only and performs an idempotent content-hash-aware upsert; a missing artifact remains a safe no-op while partial, stale, malformed, or provenance-mismatched artifacts are rejected.
- `app/services/learning_aid_view.py` deterministically maps stored JSON into bounded display strings (`英文搭配｜中文义`, `单词 (词性)｜中文义`) and resolves examples so curated words keep `Word.example` while open words use the validated aid example. `review_service.py` preserves aid-row presence and any existing local issue category alongside these values; the review card, review page, and wordbook page never parse raw JSON, query SQLAlchemy, or call a model adapter. Multiple collocations or word-family entries are rendered one per line for scanability.
- `packaging/CET-Agent.spec` bundles the formal JSONL/provenance and relation-candidate JSONL/provenance plus the source manifest as read-only resources.

### Verified adaptive lexical facts

- `app/ai/schemas.py` defines a strict discriminated union for noun number/countability, verb paradigms, adjective/adverb degree, numerals, pronouns, and per-surface pronunciation/region/register/sense/notes. Grouped synonym/antonym/derivative relations retain part of speech and sense; grammatical inflections are never stored as `word_family` entries.
- `data/word_lexical_facts.jsonl` is a deterministic, versioned complete artifact for all 4,611 bundled headwords. `scripts/generate_lexical_facts.py` uses only audited irregular tables and bounded spelling rules; unresolved entries remain `missing` and are not guessed. Current report: 3,869 records with a typed form, 742 unresolved form records, and 3 source-validated relation groups. `less` explicitly stores `little → less → least` plus the `more` antonym; the count-noun chain `few → fewer → fewest` is represented by the same schema when such a headword is supplied.
- `app/ai/lexical_fact_validation.py` and `scripts/validate_lexical_facts.py --require-complete` validate every record's source identity, typed forms, grouped relations, canonical content hash, exact source set, and provenance/source hashes before persistence. The artifact provenance records the bundled vocabulary hashes, licenses, transformation, and coverage; no API key or model output is involved.
- `app/db/lexical_fact_seed.py` imports non-empty formal projections into the schema-v13 `word_lexical_facts` table and separately imports the validated relation-candidate overlay into `candidate_relations_json`. Both paths are hash-aware and idempotent. Candidate targets outside the vocabulary are display-only and never create a `Word`, LearningState, acquisition card, reminder, or review obligation. Complete missing formal records remain represented by the artifact/validator, while omitted runtime rows keep a fresh database compact. Schema v14 removes only older runtime placeholders that are empty in both layers; the 328 retained formal-empty rows all carry candidate-only content.
- `app/domain/lexical_query.py` indexes headwords and surface forms and answers recognized factual questions (comparative/superlative, plural, tense/participle, ordinal, synonym, antonym) locally. `app/domain/lexical_display.py` maps stored full POS names to compact learner labels (`adj.`, `adv.`, `n.`, `v.`) without changing source data. `AIService` runs this path before Provider dispatch, extracts the current card word from the bounded context, returns `deterministic-lexical-fact` for formal groups or `deterministic-lexical-candidate` for source-backed pending groups, and reports a verified-data gap instead of sending a local 3B guess. Formal facts take precedence, while additional candidate targets remain explicitly pending. Explicit advanced dispatch may still supplement a missing fact, clearly remaining unreviewed.
- `ReviewCardWidget` now accepts an optional projection of zero to four sections. A dynamic section is hidden when absent, with no empty headings or filler; verified forms/relations appear before generated collocations/family text, and the existing bounded native scroll area owns overflow. Acquisition and formal review keep answer isolation: facts enter the card/assistant snapshot only after their stage's answer boundary, while a non-testing word-detail view may show them immediately.
- Relation and derivative entries in that projection now carry aligned `LinkedWordReference` metadata. `LexicalLinkLabel` renders only the target headword as a local, escaped link; `WordDetailService` reuses the deterministic card projection for in-bank targets and emits a non-persistent reference-only fallback outside the bank. `WordDetailDialog` displays examples, trust labels, and all available sections in a scrollable popup; nested links use a Back stack, and `WordDetailController` suppresses late results by generation token.
- `packaging/CET-Agent.spec` bundles both formal lexical-fact files, both relation-candidate files, and the pinned source manifest. The source database upgrades automatically from schema v11/v12/v13 to v14; missing candidate artifacts remain a safe no-op for developer/legacy databases, while present partial, stale, malformed, or provenance-mismatched artifacts fail before the serialized write.
- `data/lexical_source_manifest.json` is the machine-readable phase-2 allowlist. It pins the exact ECDICT revision, Open English WordNet 2025, and Chinese Open Wordnet 2.0 download URLs/SHA-256 values, reviewed reuse rights, approved fields, notice requirements, and attribution text. `app/ai/lexical_source_validation.py` rejects duplicate identities/files, non-HTTPS references, hash drift, blank notices, unapproved uses, and an “approved” source lacking redistribution, modification, or commercial-use rights.
- `app/domain/lexical_source_readers.py` streams the 62.9 MiB ECDICT CSV and the two compressed WN-LMF sources without adding a runtime dependency. It accepts only documented ECDICT exchange fields, verifies embedded lexicon identity/license markers, preserves OEWN synset and explicit sense-antonym edges, and aligns English/Chinese sense labels only through their shared ILI. `app/domain/lexical_source_audit.py` owns deterministic completeness, corroboration, conflict, and strict Chinese-overlap metrics.
- `scripts/audit_lexical_sources.py` is an explicitly invoked candidate-only pipeline. It may download missing inputs into ignored `build/lexical_sources/`, verifies every hash before parsing, writes a deterministic report, and never mutates `word_lexical_facts.jsonl` or SQLite. The checked snapshot is `docs/reports/lexical_source_audit.json`; the architecture/promotion plan is `docs/LEXICAL_CARD_PHASE2_PLAN.md`.
- `app/domain/lexical_candidate_builder.py` compares each current typed role with exact ECDICT `s/p/d/i/3/r/t` values, emits field-level locators and pinned source hashes, and classifies conflicts only as review priorities (`deterministic_rule_candidate` or conservative `orthographic_variant_candidate`). `app/ai/lexical_candidate_validation.py`, `scripts/generate_lexical_fact_candidates.py`, and `scripts/validate_lexical_fact_candidates.py --require-complete` enforce exact 4,611-word coverage, source/form consistency, canonical hashes, evidence pointers, and provenance counts.
- The first audit found exact ECDICT rows for all 4,611 words, form exchange evidence for 3,906, 224 words with source candidates among the 742 current missing-form records, 9,780 corroborated current form roles, and 827 conflicts. The candidate artifact contains 11,725 role comparisons: 1,118 source additions, 9,780 agreements, and 827 conflicts; its heuristic conflict labels are 750 legacy-rule candidates and 77 possible `-l` spelling variants. OEWN covers 4,583 headwords; 4,119 have relation candidates, 3,872 have at least one Chinese ILI label, 2,753 have a strict Chinese-aligned candidate, and 1,629 have exactly one such review-ready group. These are candidate counts, not learner-facing verified facts.
- `app/domain/lexical_relation_candidate_builder.py` joins OEWN synset/explicit-antonym evidence to COW labels through shared ILI, preserves multiple aligned senses with a deterministic four-group cap, accepts single-word targets from the complete ECDICT index (including outside-bank reference words), requires positive frequency, compatible POS, and an ordinary ECDICT translation, and limits each relation type to six targets. `app/domain/lexical_relation_quality.py` parses escaped/newline ECDICT glosses, inherits POS across semicolon-separated segments, filters specialist domain markers per segment, and blocks one-character Chinese substring false matches. `app/ai/lexical_relation_candidate_validation.py`, `scripts/generate_lexical_relation_candidates.py`, and `scripts/validate_lexical_relation_candidates.py --require-complete` validate the complete 4,611-record overlay, three source pointers per item, target-specific translations, relation membership, frequency bounds, selection status, hashes, and provenance.
- The v3 relation overlay covers all 4,611 records and keeps Chinese-overlap-aligned WordNet senses, full-ECDICT single-word targets (including outside-bank reference words), positive frequency, compatible POS, ordinary-language translations, at most six targets per relation type, and at most four groups per headword. It contains 4,262 groups (3,736 synonym and 526 antonym), 8,308 targets across 4,727 unique words, with 1,539 single-sense selections, 936 multi-sense selections, 88 deterministically truncated multi-sense records, and 2,048 no-aligned-sense records. 2,383 words have at least one synonym group and 474 have at least one antonym group; 2,563 have any candidate relation group. These remain source-backed candidate counts and are learner-visible only with an explicit `来源候选 · 待审核` trust label.
- The clickable related-word detail-card milestone is implemented as a read-only first slice: relation/derivative rows retain bounded target metadata, `WordDetailService` loads an in-bank target's existing Word/aids/lexical-fact projection, and an outside-bank target is shown as an explicitly labelled reference-only card. `WordDetailDialog`/`WordDetailController` provide scrollable navigation, stale-result suppression, and close-coordinator worker tracking. The popup creates no Word rows, changes no favorites/mastery or schedule, calls no LLM, and is reachable only after the study card's existing answer boundary exposes the section. Full tests, lint, formatting, mypy, and offscreen smoke passed after this slice.
- The global vocabulary lookup slice is complete: `WordDetailService.search_words` ranks an exact local headword before a bounded ASCII prefix result set, while `VocabularyPage` performs the query off the GUI thread and never expands an empty query into the full bank. The sidebar exposes this page, and favorites, mastered words, dashboard wrong-word actions, and each analysis-cluster word open the same read-only detail controller. Outside-bank relation targets remain reference-only; no lookup action creates a study obligation.
- `scripts/audit_lexical_card_quality.py` performs the requested offline per-card replay over all 4,611 headwords and writes ignored `build/lexical_card_quality_audit.json` plus one detail row per formal form/candidate/relation item in `build/lexical_card_quality_audit.jsonl`. The latest audit checks 16,657 formal form surfaces, 11,725 ECDICT form candidates, and 8,308 relation items: 9,780 form agreements, 1,118 source additions, 827 form conflicts, 5,123 relation items with no translation flag, and zero learner-visible domain-only relation translations. It records 3,185 conservative synonym-sense mismatch flags across 1,510 headwords for human review and never auto-promotes a candidate. The installed `baoyu-translate` skill supplies the review principles (contextual sense, POS/term consistency, staged editorial review), while pinned dictionaries remain the factual sources.

### Reminder and desktop lifecycle

- `app/domain/reminder_policy.py` is deterministic and independently tested.
- Reminder input is the learned-due queue only; released but untouched new words never produce a “复习” notification. It suppresses reminders with no learned-due words, during a study session, during cooldown/snooze, before 08:00, after 23:00, or after the day's formal review work is complete.
- `app/services/reminder_service.py` persists notification, snooze, and completion state across restarts.
- Reminder evaluation reloads the authoritative runtime row inside a serialized transaction. `evaluate_and_claim` records the cooldown in that same transaction, so concurrent application instances cannot both win the same notification opportunity.
- ReminderService accepts an injectable clock so constructor state and explicit evaluations use one deterministic time source; this prevents tests and state transitions from drifting across real midnight.
- A newly due word on the same day clears a stale completed state.
- A far-future persisted notification timestamp cannot suppress reminders indefinitely after a system-clock correction; only a small rollback within the configured cooldown remains suppressed.
- Main-window reminder evaluation, snooze persistence, and post-session completion checks share one FIFO `AsyncWorker` queue. Duplicate timer evaluations are coalesced, and no reminder database operation blocks Qt event handling.
- Snooze and notification claims return an absolute persisted cooldown-expiry time. A dedicated single-shot Qt timer schedules that exact re-evaluation while the five-minute heartbeat remains as recovery for restart, sleep, clock changes, and duplicate-instance races.
- Each application process owns a renewable 10-minute active-review lease. Entering/reviewing publishes it, leaving or closing releases it, expired leases are deleted during evaluation, and any live instance lease suppresses notifications across all windows.
- `app/infrastructure/notification_adapter.py` keeps the Qt system tray and prefers Windows-Toasts on Windows. Native Toast and the in-app banner both expose “start review” and “snooze 30 minutes”; native callbacks are queued through a Qt signal bridge before touching UI state. Native initialization/send failures degrade to the Qt tray bubble.
- Installer-marked frozen builds use the dedicated `CET.Agent.Desktop` AUMID created by the Start Menu shortcut. Source/portable runs cannot claim that identity merely because an installed shortcut also exists. Every process tracks and removes only its own Toast rows during close, so dead action buttons do not remain and one instance cannot clear another instance's notification history.
- `app/ui/widgets/reminder_banner.py` remains the always-visible in-app copy of both notification actions.
- `app/ui/main_window.py` waits for active dashboard, vocabulary lookup, review, embedded-review-assistant, full-chat, analysis, linked-word detail, and reminder QThreads before destroying the window. If a finishing task starts a chained reload during deferred close, the new worker is also watched before shutdown continues. Attaching a close watcher is followed by an `isRunning()` re-check, so a worker that finishes between discovery and signal connection cannot leave the hidden window waiting forever. The smoke path closes the real window instead of terminating the event loop around live workers, and deferred close explicitly ends the application after a hidden window has released its final lease.

### Resilience and diagnostics

- Model unavailable, embedding unavailable, invalid JSON, empty data, no clusters, and SQLite busy conditions have recoverable paths.
- Failed review-queue reloads clear and disable the stale card; dashboard and analysis refresh failures display safe state instead of leaking an exception through a Qt callback.
- Analysis refresh clears detached AI output and resets stale error status before presenting a new cluster snapshot.
- Unexpected UI and startup exceptions are logged with full detail but converted to stable generic user messages; filesystem, SQL, and transport details are not rendered.
- While chat is waiting for a low-confidence routing choice, its pending question and input controls are locked so a second send cannot silently replace the first question.
- Blocking dashboard, review-database, model, and network work runs in `AsyncWorker` QThreads, not on the UI thread.
- Detailed failures go to logs; user-facing messages remain concise.
- Root logging configuration is serialized within the process, preventing concurrent bootstrap calls from registering duplicate rotating-file and console handlers.
- pytest cacheprovider is disabled by `pytest.ini` to avoid cache-directory write failures in restricted environments.
- `scripts/validate_local_ai.py` is the repeatable P0 integration check for live chat, cached embeddings, semantic graph rebuild, structured cluster output, and AI cache reuse. Its latest cold and warm runs both exited successfully.

### Delivery engineering

- `requirements.txt` and `requirements-dev.txt` declare compatible direct dependencies; the corresponding `.lock` files pin the complete tested runtime and development dependency trees. Regenerate them with pip-tools 7.6.1 after changing a direct dependency.
- `pyproject.toml` centralizes the Python 3.11 Ruff and Mypy baselines. The entire Python tree is Ruff-formatted; CI rejects lint, format, or application/script type-check drift.
- `.github/workflows/ci.yml` runs on Windows for Python 3.11 and 3.13 with read-only repository permission. It installs the development lock, runs `pip check`, Ruff lint/format, Mypy, all tests, and the offscreen startup smoke. No Git remote is configured yet, so the workflow is locally validated but has not had a hosted run.
- `packaging/CET-Agent.spec` builds a windowed PyInstaller 6.22.0 onedir release, includes the Windows-Toasts/WinRT extensions and license metadata plus required vocabulary/config resources, and explicitly excludes the unused `fsrs.optimizer` scientific-training stack. CI builds and launches the package with isolated `LOCALAPPDATA`, then asserts writable database, log, and config-template paths.
- `packaging/CET-Agent.iss` and `scripts/build_windows_installer.py` create an unsigned current-user Inno Setup installer from the onedir tree. It uses a stable AppId, installs below `%LOCALAPPDATA%\Programs\CET-Agent`, creates a dedicated-AUMID Start Menu shortcut and optional desktop shortcut, and intentionally leaves `%LOCALAPPDATA%\CET-Agent` learning/config/log data intact on uninstall.

## 5. Verification ledger

Update this section after every material change. Never report a feature as verified based only on code inspection.

| Verification | Latest result | Evidence date |
|---|---:|---:|
| Full pytest suite | 421 passed in 31.19s with schema-v14 cleanup plus schema-v13 candidate import/query/UI coverage, target-focused relation formatting, candidate chat labeling, translation-quality gates, expanded relation overlay, linked-word detail-card regressions, confusion-cluster practice, global vocabulary lookup, dashboard route actions, session summaries, validated workload settings, decomposed session-configuration/summary helpers, and the v14 placeholder-pruning regression | 2026-08-24 |
| Three-stage acquisition and mastered-word implementation | 23 focused domain/service/migration/seed/UI/content tests passed: persistent 0→3 transitions, wrong-answer retention, direct confirmation, FSRS-field preservation, serialized concurrent-attempt protection, exact +24-hour graduation, v10→v11 tables, legacy adoption, full mastered exclusion/restoration including learned-due recovery, reminder suppression, stale confusion-edge filtering, group-boundary/next-group gating, explicit five-word unlock path, three-stage off-GUI UI flow, recovery page, retryable content-error state, and irregular-form handling | 2026-08-23 |
| Complete acquisition cloze artifact | `python scripts/validate_acquisition_content.py` returned `PASS: 4611 acquisition records produce safe four-choice cloze items`; every record has four unique choices, exactly one correct option, and no target-form leak after repeated-occurrence masking | 2026-08-23 |
| Split acquisition, formal review, and optional practice | 73 focused service/migration/UI/lifecycle tests passed: untouched and learned-due queues are disjoint, new words use read→quiz→rate, reminders use learned-due only, yesterday/recent/wrong/favorite scopes page without repeats, PracticeLog writes leave every FSRS field/counter and ReviewLog count unchanged, all three page/assistant workers participate in deferred close, and dashboard clock warnings render safely | 2026-08-23 |
| Confusion-cluster practice loop | 4 focused service/UI regressions passed: selected cluster IDs preserve deterministic order, duplicate/mastered/untouched cards are filtered, the analysis action reaches the bounded practice page, `CONFUSION_CLUSTER` attempts write only `PracticeLog`, and FSRS schedule plus formal `ReviewLog` count remain unchanged | 2026-08-24 |
| Word learning-aid pipeline | 72 focused tests passed with no network or secret exposure: project `.env` precedence, DeepSeek JSON/non-thinking/8,192-token construction, truncation rejection, content/source/provenance/hash/model gates, curated regular-form handling, lexicalized derivative handling, response-log initialization, checkpoint identity, batch/retry/isolation/promotion, strict bootstrap import, migration, service/UI mapping, and missing-artifact degradation | 2026-08-23 |
| Verified lexical-fact pipeline | 14 focused schema/query/seed/migration/projection/UI tests passed; `python scripts/validate_lexical_facts.py --require-complete` returned 0 errors for 4,611 records, 3,869 typed-form records, 742 explicit missing-form records, and 3 relation groups; `less` returned `least` through `AIService` with an exploding fake Provider and no call | 2026-08-24 |
| Lexical source policy and coverage audit | 7 focused tests passed: license-rights rejection, pinned-file hash failure, documented exchange parsing/counts, strict Chinese overlap, exact manifest identity, and synthetic OEWN/COW shared-ILI plus explicit-antonym preservation. The live three-source audit verified all hashes and produced the checked 4,611-word snapshot: ECDICT 4,611 exact rows/3,906 form candidates/9,780 corroborated roles/827 conflicts; OEWN 4,583 headwords/4,119 candidate words; COW-assisted strict alignment 2,753 words/9,931 synonym edges/623 antonym edges; no formal artifact or database mutation | 2026-08-24 |
| ECDICT field-level form candidates | 5 focused tests passed; `python scripts/generate_lexical_fact_candidates.py` and `python scripts/validate_lexical_fact_candidates.py --require-complete` produced and independently validated 4,611 candidate records/11,725 role comparisons: 1,118 source additions, 9,780 agreements, and 827 classified conflicts (750 legacy-rule candidates/77 possible `-l` variants). Every candidate retains ECDICT version, exchange code locator, source hash, manifest hash, canonical content hash, and candidate-only status; formal artifact, SQLite, and UI remained unchanged | 2026-08-24 |
| WordNet/COW relation candidate overlay | 8 focused pipeline/translation-quality tests passed; `python scripts/generate_lexical_relation_candidates.py` and `python scripts/validate_lexical_relation_candidates.py --require-complete` produced and independently validated the final v3 4,611-record overlay: 1,539 selected single-sense, 936 multi-sense, 88 truncated multi-sense, 2,048 no-aligned-sense records, 4,262 groups/8,308 targets across 4,727 unique targets. 2,383 words have synonym groups and 474 have antonym groups; target-specific ordinary ECDICT translation evidence is retained, specialist-only targets are rejected, and candidate data is visibly imported only with `来源候选 · 待审核` trust | 2026-08-24 |
| Per-card lexical quality audit | `python scripts/audit_lexical_card_quality.py` passed with 0 errors/0 hard failures over all 4,611 records; ignored JSON/JSONL report contains 16,657 formal surfaces, 11,725 form candidates, 8,308 relation items, 827 form conflicts, 3,185 conservative synonym-sense mismatch flags across 1,510 headwords, and zero learner-visible domain-only relation translations | 2026-08-24 |
| Target-focused relation display | 31 focused lexical-fact/query/chat/UI tests passed: formal and pending relation items now render only `近义/反义：目标词 adj./adv./n./v. 翻译`, always show target POS even for one sense, omit the current headword's POS/sense, clean source token markers, retain pending trust in status/metadata, and preserve zero-Provider deterministic answers | 2026-08-24 |
| Linked-word detail card | 6 focused service/widget/controller tests passed: in-bank targets reuse full card facts, outside-bank targets remain non-persistent reference-only cards, invalid targets are rejected, links are HTML-escaped/local-only, study-card callbacks fire, and stale asynchronous generations are ignored | 2026-08-24 |
| Word-detail navigation polish | 2 new focused UI/service regressions passed: relation references retain the originating word/meaning/relation, the dialog renders deterministic two-column comparison context, keyboard back navigation is available, and linked-label focus can be restored in a small 480×400-capable dialog without changing trust or persistence semantics | 2026-08-24 |
| Validated workload settings | 4 focused config/service/UI/migration regressions passed: daily release limit is validated and applied in frequency order only on first untouched-level activation, group/extra-study values are bounded and threaded into both study pages, settings explains scope and restart behavior, and reviewed/activated state remains unchanged | 2026-08-24 |
| UI session-boundary decomposition | 2 focused pure-helper regressions plus the full review/acquisition/learning-aid UI suite passed: session mode/issue-choice constants and completion-summary formatting are isolated into dependency-light modules, with no worker/database ownership changes | 2026-08-24 |
| Global vocabulary lookup and detail entry points | 7 focused service/UI regressions passed: exact headword ranks before bounded prefixes, invalid/empty queries do not load the whole bank, the sidebar lookup page opens read-only local cards, favorites/mastered/dashboard wrong-word actions and each analysis-cluster word expose the same controller, and completed detail workers detach before deferred deletion | 2026-08-24 |
| Actionable dashboard and session summaries | 4 focused UI regressions passed: new/due metric actions navigate through callbacks, acquisition summaries show attempts/mistakes by stage/new graduates/first formal due, formal review summaries show correct/needs-strengthening counts and earliest next due, and practice summaries state recall outcomes without changing scheduling | 2026-08-24 |
| Current PyInstaller onedir build | `python -m PyInstaller --noconfirm --clean packaging/CET-Agent.spec` completed successfully with the current schema-v14 resources, vocabulary artifacts, licenses, and Windows notification dependencies; output is `dist/CET-Agent/` | 2026-08-24 |
| Current Inno Setup installer build | `python scripts/build_windows_installer.py --compiler C:\Users\Admin\AppData\Local\Programs\Inno Setup 6\ISCC.exe` completed successfully with Inno Setup 6.7.3; output is `dist/installer/CET-Agent-Setup-0.1.0.exe` | 2026-08-24 |
| Installed/distributed smoke | Both `dist/CET-Agent/CET-Agent.exe --smoke-test` and the installed `build/installer-smoke-v14/CET-Agent.exe --smoke-test` exited 0 in isolated `LOCALAPPDATA` roots; each created writable schema-14 databases with 4,611 words/4,198 lexical rows, `PRAGMA integrity_check=ok`, and no foreign-key violations | 2026-08-24 |
| Python dependency integrity | `pip check` reported no broken requirements | 2026-08-24 |
| Ruff static check | all checks passed across app/scripts/tests/main | 2026-08-24 |
| Ruff format gate | 166 files already formatted | 2026-08-24 |
| Mypy application/scripts check | exit code 0; 106 source files | 2026-08-24 |
| Learning-aid state, trust feedback, and responsive UI | 58 focused migration/service/review/UI regressions passed: missing aid versus valid empty family remains distinguishable, AI-unreviewed status is visible, six bounded feedback categories persist idempotently in schema v9, absent-aid reports are rejected, existing feedback reloads with the card, and aid columns switch between horizontal and vertical layouts at the tested width boundary | 2026-08-23 |
| Scrollable narrow learning-aid content | 22 focused review/learning-aid UI tests passed; the long-content regression resizes a populated card from 800px to 560px, asserts a positive vertical scroll range with `ScrollBarAsNeeded`, sends a real downward wheel event, and observes the scrollbar advance | 2026-08-23 |
| Compact trust badge and embedded distinction repair | 70 focused routing/prompt/service/chat/review/learning-aid UI tests passed: the trust state is a non-wrapping short badge with full tooltip disclosure, current validated collocations/family enter the bounded card snapshot, `混淆` routes locally, and the quick distinction request receives a grounded usage-boundary task. A live contextual request reached Ollama with HTTP 200 and returned non-degraded from `qwen2.5:3b` | 2026-08-23 |
| Non-blocking dashboard/review UI | worker-thread identity, rendered dashboard result, refresh coalescing, safe failure state, asynchronous queue load/submission, batch reload, and active-worker tracking passed; 19 focused tests passed | 2026-08-22 |
| Review UX phase/guard/undo | 34 focused service/UI/migration tests passed: exact FSRS/counter restoration for correct and Again reviews, deletion of the reverted log, later-review rejection, schema-v6 column upgrade, phase/progress rendering, 450 ms number-key guard, and restored-card reopening | 2026-08-22 |
| Favorite wordbook, learning-aid shell, and sidebar palette | 40 focused migration/service/review/wordbook/main-window/theme tests passed: idempotent add/remove, unknown-word rejection, persisted queue state, schema-v7 creation, asynchronous list/removal, stale-load clearing, review toggle, post-answer no-fabrication placeholders, shutdown worker tracking, and explicit high-contrast sidebar selectors | 2026-08-22 |
| Serialized reminder tasks and atomic claim | two concurrent ReminderService instances produced exactly one notification winner; FIFO task order and non-GUI thread identity passed; snooze/day rollover and persisted completion regressions passed | 2026-08-22 |
| Exact reminder wake-up | persisted Snooze and claimed-notification cooldowns expose one absolute 30-minute expiry; UI millisecond conversion is exact, non-negative, and Qt-bounded; restart evaluation reconstructs the same target; focused reminder/UI tests passed | 2026-08-22 |
| Actionable Windows notifications | four focused tests passed: exact Start/Snooze action payloads, background-thread callback delivery on the Qt GUI thread, frozen install-marker/AUMID selection, native-send failure fallback, and per-process Toast cleanup. A live installed Toast was present under `CET.Agent.Desktop` with both exact action arguments; process close reduced that history from 1 to 0 | 2026-08-22 |
| Manual native-notification actions | the user physically clicked both actions in the real Windows notification flyout and confirmed that “start review” and “snooze 30 minutes” both worked without issue | 2026-08-22 |
| Cross-instance active-review lease | independent owners coexist; releasing one preserves another; observer notifications are suppressed; expired leases are removed; hidden review-worker results cannot republish activity | 2026-08-22 |
| AI capacity budgets | oversized question refusal, 4,000-character chat truncation, 32,000-character structured raw guard, bounded schema fields/lists/options, 2,048-token Provider requests, and 200/300-block UI documents passed | 2026-08-22 |
| Bounded in-session chat context | four-exchange/6,000-character budgets, complete-pair retention, prompt role order, and second-question UI context propagation passed; history remains memory-only | 2026-08-22 |
| Word-scoped embedded-assistant history | 57 focused chat/conversation/routing/formal-review/acquisition UI tests passed. The regression first proved that the second word received its correct card context while also receiving the preceding word's exchange; contextual panels now send empty history after a word-label change, preserve same-word follow-ups, and leave full-page chat continuity unchanged | 2026-08-23 |
| Card-bound assistant UI and deterministic quick memory | 64 focused chat/AI/conversation/acquisition/formal-review tests passed: immediate transcript/history clearing from both study pages, stale previous-card result suppression, same-card continuity, missing/hidden-context zero-call refusal, full-page history isolation, distinct escaped user/assistant/status/error rich-text colors, zero-Provider core-meaning/collocation/cloze/family/ten-second memory output, no-example fallback, and explicit-only advanced dispatch. An offscreen 430×620 render was visually inspected for the four palettes | 2026-08-23 |
| Explicit advanced channel and lexical expansion | 90 focused config/factory/routing/prompt/service/chat/settings/conversation tests passed: exact-host DeepSeek key reuse without cross-endpoint leakage, missing-key rejection, visible three-mode selector, explicit advanced bypass of auto confirmation, unavailable-option gating, singular/plural synonym/antonym routing with metalinguistic scope override, grounded local refusal to invent card content, deterministic unreviewed-content labeling, rich lexical task prompt, secret-free settings status/template, and narrow settings-page scrolling. A live `deepseek-v4-flash` contextual request was non-degraded in 8.0s, contained `primary`, and returned six bounded `main` alternatives with usage distinctions | 2026-08-23 |
| Wider study layout and local-to-advanced retry UX | 56 focused Chat/Analysis/main-window/formal-review/acquisition UI tests passed: 1280×760 default and 960×620 minimum geometry, 400-pixel initial/340-pixel minimum contextual assistant, no user-facing confidence in chat or analysis, local-only successful retry offer, exact question/card/pre-local-history reuse, rejected-answer exclusion, deterministic-result suppression, and card-transition invalidation | 2026-08-23 |
| Integrated learning workspace | 47 focused deterministic/service/UI regressions passed: unique four-choice construction and insufficient-option fallback, objective outcome logging, wrong-answer Again guard, five-word continuation with selected-level/history/due-priority protection, current-word assistant snapshot, shortcut isolation, and embedded-worker shutdown tracking | 2026-08-22 |
| Real vocabulary quiz queue | 28 current CET4 due cards all received four choices; five background queue loads ran in 0.374–0.417s, maximum 0.417s | 2026-08-22 |
| Concurrent fresh bootstrap | three repeated two-worker runs on separate fresh databases all returned 4,611 words and one activation per worker; regression test also passed | 2026-08-22 |
| Bootstrap/logging failure safety | forced schema-upgrade failure disposed its Database; simultaneous logging configuration registered exactly one file/console handler pair | 2026-08-22 |
| Per-level first activation | fresh CET4 rebased exactly 3,320 untouched open words once; later CET6 activation independently rebased 1,278; repeat startup preserved the original activation | 2026-08-22 |
| Legacy level adoption | open-word review history preserved every due time and adopted its earliest review timestamp; curated/demo-only history did not suppress first real level activation | 2026-08-22 |
| Deterministic chat routing | regression cases cover local vocabulary/grammar/distinction including `混淆`, Unicode normalization, advanced confirmation, empty/general/off-topic refusal, English word boundaries, guarded scope override, zero-call refusal, and ChatPanel preflight that sends available formal/candidate lexical facts locally before advanced confirmation | 2026-08-24 |
| Provider response-shape degradation | non-object and malformed nested OpenAI-compatible chat JSON plus non-object Ollama Embedding JSON normalized to project unavailable exceptions | 2026-08-22 |
| Current-window confusion analysis | future-only repeat errors produced 0 candidates, 0 edges, and 0 clusters; current cluster counts remain window-aligned | 2026-08-22 |
| Bundled metadata upgrade | corrected phonetic/meaning/example/level/frequency applied to an existing word while its ID, review count, stability, and due time remained unchanged | 2026-08-22 |
| Advanced Provider bootstrap | disabled/default, independent OpenAI-compatible construction, invalid/incomplete configuration rejection, explicit advanced dispatch, and API-key repr redaction passed | 2026-08-22 |
| Live advanced-provider dispatch | explicit advanced choice returned a non-degraded 283-character response from independent `qwen2.5:3b` while the local Provider pointed to an unreachable endpoint | 2026-08-22 |
| 100-candidate graph performance | 100 errors per candidate and all 4,950 edges: 0.932s, 0.907s, 0.913s; median 0.913s; exact one-cluster invariant passed | 2026-08-22 |
| Locked dependency install | clean Python 3.13 virtual environment installed `requirements-dev.lock`; `pip check` passed; Python 3.11/win_amd64 wheel-resolution dry run passed | 2026-08-22 |
| GitHub Actions workflow | Windows Python 3.11/3.13 matrix defined with current official v7 checkout/setup actions; local-equivalent full gate passed; hosted run pending remote configuration | 2026-08-22 |
| Schema migration matrix | Existing migration coverage plus schema-v14 fresh/upgrade/import/cleanup columns passed: fresh, pre-version adoption, v1→v2 FSRS mapping, v2→v3 level activation, v3→v4 review leases, v4→v5 demo-state repair plus successful four-choice rating persistence, v5→v6 undo snapshots, v6→v7 favorite wordbook, v7→v8 word learning aids, v8→v9 local learning-aid feedback, v9→v10 optional practice logs, v10→v11 acquisition/mastery tables, v11→v12 lexical-fact table, v12→v13 candidate columns, v13→v14 empty-placeholder pruning with candidate/formal preservation, idempotency/newer-version guard, transactional rollback | 2026-08-24 |
| Official FSRS-6 reference vectors | initial Again/Hard/Good/Easy plus five-review sequence passed against py-fsrs 6.3.2 | 2026-08-22 |
| Deterministic randomized algorithm invariants | 6,000 checks passed | 2026-08-22 |
| Concurrent review/AI/Embedding focused regression | 15 tests passed in five consecutive runs; no lost update or cache exception | 2026-08-22 |
| Open vocabulary artifact | deterministic rebuild hash matched `1afc9925…9a16f`; 4,598 unique rows, 3,320 CET4 + 1,278 CET6, Chinese meaning and phonetic coverage 100%, no curated overlap | 2026-08-22 |
| Bundled vocabulary import | 4,611 total words/states; second import inserted 0; invalid/duplicate/out-of-range rows rejected before mutation | 2026-08-22 |
| Offscreen startup/shutdown smoke | fresh temp-database startup/import and source-mode initialization exited 0 at schema v14; runtime source database retains 4,198 lexical-fact rows (3,870 formal non-empty plus 328 candidate-only formal-empty) and 2,563 candidate projections; repeated startup is idempotent | 2026-08-24 |
| Live local-AI validation after schema v13 | `python scripts/validate_local_ai.py` exited 0 with Ollama chat/embedding HTTP 200 responses, cached vectors, 8 graph candidates/5 edges/3 clusters, and structured-analysis cache hits; candidate lexical queries were separately verified with an exploding Provider and no model call | 2026-08-24 |
| Full DeepSeek generation and promotion | 4,611/4,611 records passed the complete validator with 0 failures; artifact SHA-256 `97d1a2d4c4de6fb28c36ebbc2eeed6ebf1ee1eab8bc6d9441abba14433ef616f`; model `deepseek-v4-flash`; 3,329 CET4 + 1,282 CET6, 13 curated + 4,598 open, 4,598 AI-generated examples, 740 empty word-family results | 2026-08-23 |
| Full artifact validator | `python scripts/validate_word_learning_aids.py --require-complete` returned `errors=0` and matching provenance/source hashes/statistics | 2026-08-23 |
| Multi-item learning-aid layout | Review-card regressions confirm multiple collocations/word-family entries render as newline-separated lines rather than middle-dot separators, stack vertically when narrow, and remain accessible through a mouse-wheel scroll viewport when their content exceeds the bounded display height | 2026-08-23 |
| SQLite integrity, persisted activity, and foreign keys | live source database is schema v14 with 4,611 words/LearningStates/acquisition states, 4,198 lexical-fact rows, 113 ReviewLogs, 33 PracticeLogs, 39 acquisition attempts, one favorite, zero mastered/feedback rows, and 2,563 source-backed candidate projections; `PRAGMA integrity_check=ok` and foreign-key violations=0 after the serialized cleanup, with 2,383 synonym-bearing and 474 antonym-bearing candidate rows in the v3 artifact | 2026-08-24 |
| Runtime demo-state repair | pre-v5 backup created under ignored `data/backups`; six impossible reviewed-without-time states became zero; `adapt` retained exactly its one real correct review; 28 demo ReviewLogs remained; repaired `adept` and preserved `adapt` both produced valid FSRS schedules | 2026-08-22 |
| Demo graph | 7 candidates, 5 edges, 3 clusters | 2026-08-22 |
| Ollama chat through project provider | cold 29.490s; warm 1.246s; non-degraded Chinese response | 2026-08-22 |
| Ollama embedding through cached provider | 2 vectors, dimension 768; cold 20.159s; cache rows 0→2→2 | 2026-08-22 |
| Semantic graph rebuild | 7 candidates, 5 edges, 3 clusters; `embedding_available=true` | 2026-08-22 |
| Structured cluster JSON and AI cache hit | first live result `cached=false`; second `cached=true`; Pydantic passed | 2026-08-22 |
| Latest warm full local-AI validation | exit code 0 on schema v5 repeat startup; chat 1.087s; embedding cache rows remained 7→7→7; graph 7 candidates/5 edges/3 clusters; structured analysis cached true→true | 2026-08-22 |
| Live contextual review answer | `adapt` returned a non-degraded 3B scene wrapped by its exact example/meaning hook and deterministic `Students ____ quickly to new environments.` item; no-example `government` returned an exact stored word/meaning hook plus `n. 政府, 内阁 → ____`; invented hook content was excluded in both paths | 2026-08-22 |
| Live uncached bounded structured output | `accept/except` analysis under the 2,048-token Provider limit returned non-degraded schema-valid output for both words; 829 JSON characters | 2026-08-22 |
| Frozen writable-path resolution | source/frozen/local-app-data/home-fallback/relative-database cases plus non-overwriting `.env.example` install passed | 2026-08-22 |
| Windows onedir package | schema-v5/grounded-memory rebuild includes Windows-Toasts/WinRT binaries and distribution license metadata; isolated smoke exited 0, created schema v5 with 4,611 words and `integrity_check=ok`, installed `.env.example`, and left the package directory state-free | 2026-08-22 |
| Unsigned Windows installer | Inno Setup 6.7.3 compiled the final schema-v5/grounded-memory `CET-Agent-Setup-0.1.0.exe` (44.70 MiB, SHA-256 `D46E6300B42EA162D5D06247D441271F015C614A7FF2296B1DDF651A644A58C9`, intentionally `NotSigned`). The unchanged current-user install/uninstall flow previously passed executable/shortcut/registry creation, installed smoke, and byte-for-byte learning-data preservation | 2026-08-22 |
| Visible Windows desktop flow | prior detailed navigation/reminder/review/analysis/AI/settings/close tests passed; in the current acceptance pass the user confirmed all currently implemented functions behaved without issue. The latest real post-v5 record saved `continue` with `rating=EASY` and `question_type=meaning_choice_correct`; no ERROR/WARNING appears after the first schema-v5 startup | 2026-08-22 |

Baseline commands:

```powershell
Set-Location 'D:\work\english'
python -m pip install -r requirements-dev.lock
python -m pip check
python -m pytest -q
python -m ruff check app scripts tests main.py
python -m ruff format --check app scripts tests main.py
python -m mypy
$env:QT_QPA_PLATFORM='offscreen'
python main.py --smoke-test
python scripts/create_demo_data.py
python scripts/benchmark_confusion_graph.py
python scripts/validate_acquisition_content.py
python scripts/validate_lexical_facts.py --require-complete
python scripts/audit_lexical_sources.py
python scripts/generate_lexical_fact_candidates.py
python scripts/validate_lexical_fact_candidates.py --require-complete
python scripts/generate_lexical_relation_candidates.py
python scripts/validate_lexical_relation_candidates.py --require-complete
python scripts/validate_local_ai.py
& 'C:\Users\Admin\AppData\Local\Programs\Ollama\ollama.exe' list
python -m PyInstaller --noconfirm --clean packaging/CET-Agent.spec
python scripts/build_windows_installer.py
python main.py
```

## 6. Known incomplete work

### Current defect status

- No reproducible scheduling, persistence, or study-route defect is open after separating untouched learning, learned-due review, and non-scheduling practice; correcting the learning-aid empty-state ambiguity and narrow-window content clipping/status wrapping; repairing the embedded distinction action's pre-Provider routing refusal; eliminating cross-word embedded-assistant history leakage; adding the verified lexical-fact interception; publishing the source-backed relation overlay; and pruning empty runtime placeholders through schema v14. `less` now returns `least` from the typed local artifact with zero Provider calls, while candidate-only words such as `adept` return deterministic WordNet/COW suggestions without calling qwen2.5:3b. The current source database is upgraded to schema v14; semantic quality of AI-generated examples/collocations remains a separate review concern.
- The previously observed 20 future-clock ReviewLogs have naturally aged into the past as the clock advanced; the 2026-08-24 audit finds zero real ReviewLogs more than five minutes ahead of current UTC time. No history was rewritten. The dashboard warning/exclusion path remains implemented for any future clock jump or imported anomaly.
- Low-risk database hygiene is complete for the current source database: schema v14 removed 413 legacy `word_lexical_facts` rows that were empty in both formal and candidate layers, retained 328 rows carrying candidate-only content, and verified idempotency plus SQLite integrity/foreign-key checks. Fresh databases and subsequent imports remain compact; no ad-hoc delete is allowed.
- No form or relation candidate has been promoted to `source_validated`. The v3 relation overlay is packaged, imported, queried, and displayed only as `来源候选 · 待审核`; it is isolated from the formal artifact and all scheduling data. The offline audit has checked every relation/form item, removed the reported `human → homo` specialist-only target, and produced a detail ledger. A stratified human audit of the 827 form conflicts, 1,118 source additions, and 3,185 conservative synonym-sense mismatch flags across 1,510 headwords remains mandatory before any promotion, but the user has explicitly deferred manual AI-content review, editorial promotion work, and trust-label redesign for the current development sequence. Existing statuses and labels must remain unchanged while this work is parked.

### Content and model limitations

- The 4,598 open-vocabulary entries now have generated, validated examples, but all generated aids remain `ai_generated_unreviewed`; semantic commonness and translation quality still require sampled human review before being treated as authoritative.
- `qwen2.5:3b` remains a small local model. The default compact “怎么记” action no longer uses it, while simple example explanations remain acceptable; open-ended deep grammar/usage/distinction answers may still be shallow. Recognized morphology/relation questions with no imported verified fact now stop locally instead of guessing; an explicitly selected advanced Provider may supplement such a gap, but that output remains unreviewed. The independent DeepSeek advanced Provider is enabled on this workstation and available through every chat selector.
- The complete artifact contains 740 entries with an intentionally empty word-family list; this is allowed by the “quality over quantity” contract and is now shown as “暂无可靠的同族 / 派生词”, not as pending generation. Collocations and examples are present and validated structurally, but semantic commonness, translation quality, and usefulness still require sampled human review. The new local issue report records user findings but is not itself an editorial correction or a completed review workflow.
- The formal `data/word_learning_aids.jsonl` and `data/word_learning_aids.provenance.json` are now present and imported. The Git-ignored `D:\work\english\.env` remains the only location of `DEEPSEEK_API_KEY`; generated outputs and logs contain no API key.
- The formal lexical-fact artifact still has 3,869/4,611 form records, only three curated relation groups, and 742 unresolved form records; phase-2 auditing has deliberately not promoted any candidate to verified status. The candidate-only ECDICT artifact covers all 4,611 records and 11,725 role comparisons: 1,118 source additions, 9,780 agreements, and 827 conflicts (750 legacy-rule review candidates and 77 possible `-l` spelling variants). Samples include clear rule failures (`admit → admited`, `arise → arised`, `abandon → abandonning`), but also regional alternatives and polysemy, so ECDICT cannot silently overwrite any conflict. The final v3 WordNet/COW overlay covers all 4,611 records with 2,383 synonym-bearing words, 474 antonym-bearing words, 4,262 groups, and 8,308 targets; 2,048 records remain without a Chinese-aligned sense. Candidate rows are now imported into schema-v13 only as `candidate_only`, displayed with a pending-review label, and never treated as formal facts. A fresh runtime stores formal non-empty projections plus the candidate overlay; schema v14 removes only the 413 historical rows that had no candidate content, while 328 candidate-only formal-empty rows remain by design.

### Adaptive lexical-card facts (phase 1 implemented; phase 2 source audit and candidate pilots complete)

The product goal is to answer common word-specific questions proactively on the card so the learner usually does not need to ask an LLM. This is a new verified lexical-fact layer plus a separately trusted source-backed candidate overlay, not an expansion of unreviewed `word_family` text and not a prompt-only repair. The formal artifact, schema-v13 import, isolated candidate columns/import, deterministic query path, projection, dynamic card, answer gating, and regression tests are implemented. Phase 2 now has a verified source allowlist, full candidate audit, a complete field-level ECDICT form-candidate artifact, and an expanded WordNet/COW relation overlay. The stratified human audit and explicit promotion decision described in `docs/LEXICAL_CARD_PHASE2_PLAN.md` remain prerequisites for future promotion but are explicitly deferred; near-term work should follow the active optimization backlog below. Reproducibly extracted dictionary data remains primary evidence and model generation remains an explicitly unreviewed explanatory layer.

UI composition and visibility:

- The post-answer card may contain any subset of `词形`, `搭配`, `近反义`, and `派生词`. Section presence is driven by bounded verified content for that word and sense; the UI must not render a fixed four-column shell, an empty heading, “暂无” filler, or an irrelevant section merely for visual symmetry.
- `词形` has first display priority, followed by `搭配`, `近反义`, and `派生词`. One section uses the available width; two may share a row on a wide card; three or four use a responsive grid and stack vertically at the existing narrow-width breakpoint. The bounded native scroll area remains the overflow owner. Critical irregular forms must be visible without ellipsis; longer relation lists may expose an explicit local “查看更多” action without requiring AI.
- `apple` should not receive a low-value near-/antonym block merely because a model can suggest one. It may show a compact noun-number fact such as `复数 apples` plus useful collocations, while absent relation/derivative sections disappear completely. A function word with no useful paradigm may omit `词形`; `less` should show its degree chain and `few/fewer/fewest` trap; `child`, `sheep`, and `information` should show irregular, invariant, or uncountable noun behavior; `go` and `read` should show verb paradigms (including pronunciation where spelling is ambiguous); `one` should expose the cardinal/ordinal relation `one → first`.
- Acquisition and formal-review answer isolation remains mandatory: no form, synonym, antonym, derivative, or collocation that can reveal a recognition/cloze/spelling answer appears before the attempt boundary. The same verified facts may be always visible in a non-testing word-detail/wordbook view. Card facts also enter the bounded assistant snapshot only after they are eligible for display.
- Trust belongs to each section, not one blanket badge. Source-validated morphology/relations use a verified state; source-backed relation candidates use `来源候选 · 待审核`; existing generated examples/collocations/family explanations remain `AI · 未审核` until reviewed. Hiding an absent section must not imply that a fact was checked and found impossible; the projection must preserve the difference between missing data, a pending candidate overlay, and a validated empty set for diagnostics even though none produces an irrelevant learner-facing heading.
- Relation display is target-focused: each formal or pending item uses `近义：目标词 词性 翻译` or `反义：目标词 词性 翻译`, with standard compact POS labels (`adj.`, `adv.`, `n.`, `v.`). The current study word's own POS and sense are not repeated in the item; pending trust remains in the section badge and deterministic assistant metadata.

Typed morphology contract:

- Grammatical inflection is separate from morphological family/derivation. `worked`, `children`, `better`, and `first` are form facts; `worker`, `childhood`, `improve`, and `firstly` may be derivatives. Never put a plural, tense, participle, degree form, ordinal, pronoun case, or other grammatical paradigm into `word_family` merely to make it visible.
- The lexical artifact should use a discriminated union of bounded form records rather than one free-text string: noun number/countability (`regular`, spelling-change, irregular, invariant, plural-only, normally uncountable), verb paradigm (base, third-person singular, past, past participle, present participle), adjective/adverb degree (positive, comparative, superlative, per-sense alternatives and non-gradable notes), numeral paradigm (cardinal, ordinal, spelling/compound rule), and pronoun paradigm where applicable (subject, object, possessive determiner/pronoun, reflexive).
- Every surface form may carry an optional pronunciation, regional/register label, usage sense, and note. The model must preserve valid alternatives such as regional past forms rather than selecting one by guess. Suppletive and polysemous cases must be represented explicitly: `little → less → least` applies to quantity/degree, while adjectival small-size `little` may use `littler → littlest` with a usage note that `smaller → smallest` is often preferred; `read` needs tense-specific pronunciation even though the spelling is unchanged.
- Display relevance is deterministic. Irregular, invariant, spelling-changing, pronunciation-changing, countability-sensitive, multi-variant, easily confused, and CET-useful paradigms are mandatory. Predictable regular forms may appear as one compact line when useful, but must not force a large section or displace higher-value material.

Data, service, and trust boundary:

- The separate, versioned `data/word_lexical_facts.jsonl`, provenance, independent strict validator, schema-v13 `word_lexical_facts` table, full-artifact import gate, existing-Word matching, idempotent content-hash upsert, and isolated candidate relation columns/import are implemented. Do not mutate the existing generated-aid artifact to retrofit a different trust class.
- Source versions, hashes, transformations, coverage counts, and redistributable licenses must be pinned before committing data. The preferred primary sources are ECDICT after exact-version license/provenance verification, genuinely public-domain editions, CC0/rightsholder-waived datasets, and permissively licensed open lexical resources whose redistribution duties are compatible with this repository. MIT-style data still requires preservation of its copyright/license notice; “copyright expired” must be established for the exact edition and jurisdiction, and a modern transcription, annotation, translation, example set, or database arrangement must not be assumed public domain merely because an underlying old dictionary is. Attribution/share-alike sources may be added only after their obligations are explicitly recorded and judged compatible; commercial, API-only, no-redistribution, non-commercial, or unclear-provenance content must not enter the artifact.
- Deterministic source data and an audited irregular exception table decide forms. Sense/POS-bound lexical resources decide synonym/antonym candidates. Extract only the fields covered by the recorded source grant, retain per-record/per-field lineage where sources differ, and resolve disagreements through deterministic precedence plus an audit queue rather than silent merging. DeepSeek may translate, rank, or explain an already validated candidate set, but no local or advanced LLM may promote a form/relation to `source_validated` from its own recall.
- Synonyms and antonyms are grouped by part of speech and current sense, with a concise Chinese distinction and interchangeability boundary; never store or display a flat headword list. A relation target may sit outside the 4,611-word bank for explanation, but it must not create a `Word`, LearningState, acquisition item, reminder, or review obligation.
- A projection/service layer merges current collocations/family aids with the new typed facts and returns already formatted, ordered, visibility-qualified sections. `app/ui/` renders that projection only and never parses raw JSON or queries SQLAlchemy.
- Add a deterministic lexical-query path before every Provider call for questions such as highest/comparative degree, plural, third-person singular, past, participle, ordinal, synonym, and antonym. A verified answer is assembled locally with zero LLM calls; a source-backed candidate answer is likewise assembled locally but marked pending. A recognized question with no formal or candidate value must state that the card has no verified fact and may offer explicit advanced unreviewed supplementation; it must never fall through to 3B guessing. Generic model explanations receive exact visible facts as immutable context and cannot overwrite them.

Required implementation and verification sequence (phase 1 status):

1. Complete. Strict Pydantic schemas, provenance, typed form unions, visibility policy, and the `less`/`few` chain fixtures are present. The reproducible source transformation is offline and records source hashes/licenses.
2. Complete. The 4,611-record formal artifact, independent validator, schema-v13 migration/import, isolated candidate overlay/import, service projection, deterministic lexical-query interception, and zero-Provider tests are present before the UI integration.
3. Complete. The fixed learning-aid shell accepts dynamic 0/1/2/3/4 section projections, hides absent groups, preserves the bounded native scroll area, and keeps acquisition/formal answer isolation. Existing compatibility callers retain their old programmatic labels while learner-facing dynamic mode has no empty headings.
4. In progress, phase 2. Complete: exact-version source inventory/allowlist, rights/hash contract, ECDICT/OEWN/COW streaming parsers, shared-ILI Chinese alignment, full 4,611-word candidate audit, final v3 ordinary-gloss/POS-filtered full-ECDICT/multi-sense relation overlay, field-level form/relation candidate schemas/builders/validators, both candidate JSONL/provenance artifacts, schema-v13 isolated import/query/UI, per-card translation-quality ledger, recorded metrics, focused source/candidate/runtime tests, and the read-only linked-word detail-card slice (structured target metadata, service/dialog/controller, study-page callbacks, stale-worker suppression, and regression tests). Deferred by current user direction: stratified human review, AI-content editorial review, candidate promotion, additional issue categories tied to that workflow, and trust-label redesign. These remain required before promotion but are not the next implementation milestone. Never use model recall as source truth or silently fill a source gap.
5. Phase 1 verification complete: full pytest/Ruff/format/Mypy, complete lexical-artifact validation, schema/import/idempotency tests, dynamic-card tests, deterministic `less → least` zero-call regression, and offscreen startup all pass. A stratified human audit and coverage expansion remain required before treating all generated rule output as editorially authoritative.

### Active product and engineering optimization backlog

Manual review of AI-generated content, candidate promotion, and trust-label redesign are deliberately excluded from this active sequence. Preserve the current `已验证 / 来源候选 · 待审核 / AI · 未审核` behavior while completing the following work:

1. **Confusion-to-practice loop (complete 2026-08-24).** The deterministic confusion-analysis page now offers “练习此词簇”, opening a bounded custom word-ID practice queue. It writes only `PracticeLog` with the dedicated cluster scope and never updates FSRS, ReviewLog, reminders, or graph evidence. A side-by-side local comparison remains a later UX enhancement.
2. **Global vocabulary lookup and reusable detail entry points (complete 2026-08-24).** The sidebar/search entry for the 4,611-word local bank uses bounded prefix/exact search and the existing `WordDetailService`/`WordDetailController`. Favorites, mastered words, dashboard wrong words, and analysis clusters expose explicit/double-click read-only detail actions. Outside-bank relation targets remain reference-only and cannot become study obligations.
3. **Actionable dashboard and session-completion summary (complete 2026-08-24).** New-word and learned-due metrics now open their routes through explicit buttons, wrong words retain bounded detail actions, and acquisition/review/practice completion surfaces show bounded attempts, mistakes/recall outcomes, newly graduated words, and relevant next-due information. These summaries are UI projections only and never change scheduling decisions.
4. **Word-detail navigation polish without changing trust semantics (complete 2026-08-24).** Linked references now retain originating headword/meaning/relation metadata, the dialog shows a deterministic side-by-side source/current comparison, Alt+Left/back controls and link-focus restoration support nested navigation, and the minimum geometry is safe for small windows/high-DPI scaling. Trust labels, persistence, scheduling, and outside-bank read-only behavior are unchanged.
5. **Validated workload settings (complete 2026-08-24).** `DAILY_NEW_WORD_LIMIT`, `NEW_WORD_GROUP_SIZE`, and `EXTRA_NEW_WORD_COUNT` are bounded settings with unchanged 20/10/5 defaults. Settings explains that daily release applies only at first activation of an untouched level, while group/extra values affect future batches; reviewed/activated cards are never rescheduled.
6. **Large UI module decomposition (complete 2026-08-24, bounded slice).** Shared immutable study-session configuration and pure completion-summary formatting now live outside the page classes, reducing duplicated policy/constants without inventing a worker or card-action framework. Further page splitting remains a separate refactor only if a future feature exposes a stable boundary; current behavior and worker ownership stay unchanged.
7. **Low-priority database hygiene (complete 2026-08-24).** Schema v14 provides a serialized, idempotent cleanup that removes only formal/candidate-empty lexical-fact placeholders. The live source database removed 413 rows, retained 328 candidate-only rows, and passed integrity/foreign-key checks; no ad-hoc delete was used.
8. **Current-source distribution rebuild after functional stabilization (complete 2026-08-24).** Rebuilt the PyInstaller onedir package and unsigned Inno Setup installer from the current schema-v14 source, then smoke-tested both the onedir tree and an isolated installed copy. Application icons, Windows version resources, signing, hosted CI, and a Git remote remain separately deferred by user direction.

Recommended order is 1 → 2 → 3 → 4, then 5/6; items 7/8 are release-maintenance work rather than immediate learning-experience improvements.

### Deferred delivery work

- Application icon, Windows executable version resources, digital/code signing and its pipeline, and Git remote/hosted CI execution are explicitly deferred by current user direction. The current installer is therefore intentionally unsigned and uses default generated executable/installer resources.
- The locally installed Inno Setup 6.7.3 compiler identifies itself as “Non-commercial use only”. This is sufficient for the present local/non-commercial validation; any commercial distribution must use an appropriately licensed Inno Setup compiler or replace the installer toolchain.
- The current ignored `dist/CET-Agent/` onedir tree and `dist/installer/CET-Agent-Setup-0.1.0.exe` were rebuilt from source on 2026-08-24 and passed isolated startup smoke. They remain unsigned and intentionally use default executable/installer resources; the exact smoke runtimes are retained under `build/packaged-smoke-v14-runtime/` and `build/installer-smoke-v14-runtime/` for auditability.

## 7. Key design decisions

1. Algorithm first: structured deterministic evidence is produced before any LLM call.
2. Provider separation: LLM and Embedding endpoints are independently configurable because real deployments commonly use different servers/models.
3. Cache namespacing: endpoint identity is part of cache identity so switching providers cannot silently reuse stale model output.
4. Window consistency: candidate selection and displayed cluster error counts refer to the same configured recent period and never accept events after the evaluation time.
5. Negative cosine policy: negative cosine similarity maps to 0 rather than being shifted upward.
6. Bounded graph cost: only recent repeat-error candidates are compared, capped at 100.
7. Graceful degradation: semantic score may become 0 if embedding is offline; deterministic review remains available if all AI services are offline.
8. Structured LLM output: cluster analysis is schema-constrained, validated locally, retried once, and safely degraded.
9. Reminder persistence: cooldown, snooze, and completion survive restart; completion is invalidated when learned cards become formally due. Untouched releases are learning work, not review-reminder input.
10. Deferred close: the main window does not destroy running QThreads.
11. Explicit schema evolution: migration 1 uses SQLAlchemy metadata to create or adopt the original MVP schema; every later schema change must add the next consecutive migration. Structure changes and the stored version commit together, and an older app must reject a newer database.
12. Direct Ollama transport: localhost model calls ignore OS/environment proxies, while the separately configured OpenAI-compatible provider retains normal proxy behavior.
13. Cold-start tolerance: local Embedding uses a 60-second timeout because measured first load slightly exceeded 20 seconds.
14. Injectable reminder clock: time-dependent state coordination accepts a clock dependency; tests must not depend on the wall-clock date.
15. Conservative vocabulary licensing: generated data is distributed under CC BY-SA 3.0, with ECDICT MIT and FreeDict/WikDict/Wiktionary/DBnary attribution, exact version/hash provenance, and an explicit transformation record.
16. Dual-source vocabulary gate: ECDICT supplies display fields and CET metadata, while FreeDict independently confirms an open bilingual entry and pronunciation. A source mismatch removes the row instead of guessing.
17. Bounded initial workload: new open-data words are frequency-sorted and released independently per selected CET level at 20 per day. Level selection is a deterministic query filter, not an LLM decision.
18. Reproducible quality gate: direct dependency ranges remain human-maintainable while pip-tools lock files pin actual installs. Windows Python 3.11 and 3.13 are the CI compatibility bounds; lint, formatting, type checks, tests, dependency integrity, and an offscreen startup must all pass.
19. Explainable model routing: a pure domain policy combines normalized learning intent, word-boundary-aware English markers, precise out-of-scope phrases, text size, and task complexity into a structured route. Only explicit metalinguistic intent can override an ambiguous off-topic noun, and the UI never infers a route from a magic numeric threshold.
20. Explicit advanced-model opt-in: advanced chat is a separately configured Provider and remains disabled in the committed template. A user may invoke it for one auto-routed question by confirming, or persistently for the current UI panel by selecting the visible advanced mode; it is never chosen by an LLM. Its credentials and endpoint identity never replace or leak through the local Provider configuration.
21. Dense graph budget: scale validation uses identical deterministic embeddings and 100 synchronized errors per candidate so all 4,950 possible edges survive, covering pair scoring, practical history density, and maximum SQLite persistence cost. The repeatable script uses an ephemeral database and a conservative five-second median guard.
22. Fail-fast configuration: malformed environment values, model endpoints, graph settings, and reminder settings are rejected before use; invalid inputs are not silently replaced with defaults.
23. Monotonic review history: a review cannot be committed at or before that word's previous review timestamp.
24. Cache self-repair: malformed Embedding cache rows are discarded and regenerated, and network/model waits never occur inside an open cache transaction.
25. Evidence alignment: Pydantic shape validation is necessary but not sufficient; structured AI explanations must also match the exact algorithm-selected word multiset.
26. Current-time statistics: future-dated events are excluded from dashboard and confusion-analysis calculations rather than being trusted as learning behavior.
27. Review-session ownership: entering an active review dismisses the in-app reminder immediately; reminder content must not remain visible with a stale due count while the user is reviewing.
28. SQLite write serialization: state-dependent review writes reserve the SQLite writer before reading; `with_for_update()` alone is not treated as protection on SQLite.
29. Concurrent cache convergence: Embedding uses database upsert and AI analysis resolves a unique-key race by returning the already committed, locally revalidated winner.
30. Official deterministic FSRS: scheduling delegates to pinned `py-fsrs` 6.3.2, with fuzzing disabled and reference-vector tests. The adapter keeps the application's stable Rating/result interface while schema v2 persists the library's card phase and step.
31. Bundled metadata ownership: validated bundled word metadata may be corrected on later startup, but importing vocabulary must never replace a user's LearningState, scheduling state, or ReviewLog history.
32. Provider response shape: a syntactically valid but structurally invalid HTTP response is an unavailable-provider condition and must cross the adapter boundary as the project-defined safe exception.
33. Safe presentation boundary: unexpected exception detail belongs in rotating logs; user-visible UI receives stable action-oriented text without filesystem, SQL, or transport internals.
34. Independent level activation: a CET level's open wordbank release anchor is created only on first real activation. Only cards with no ReviewLog, no last review, and zero review count may be rebased; curated words and any learned state are immutable under this operation.
35. Convergent bootstrap: connection-level WAL negotiation receives the same bounded busy tolerance as transactional writes, every first-start mutation remains serialized and idempotent, and the bootstrap function owns disposal whenever it cannot return a usable Database.
36. Page-owned background work: each interactive page permits at most one database/model worker at a time, renders results only on the Qt thread, and exposes its active worker to the main-window deferred-close coordinator. Dashboard refreshes coalesce; review submissions preserve the current card until persistence succeeds.
37. Atomic reminder claim: the persisted runtime row is authoritative at evaluation time. A process may present a notification only after it records the cooldown under the SQLite writer reservation; local startup snapshots never decide cross-process ownership. Reminder database actions are FIFO-serialized off the GUI thread.
38. Expiring review leases: active-review suppression is a per-instance lease table rather than a singleton flag. Multiple windows may coexist, each owner can release only itself, evaluations prune expired crash remnants, and the five-minute reminder heartbeat renews a ten-minute lease.
39. Layered AI capacity budgets: deterministic routing rejects oversized inputs, Provider requests cap generation tokens, Service output is truncated or rejected before construction/cache, Pydantic caps every structured field and collection, and Qt documents prune old blocks. No single layer is trusted as the only bound.
40. Frozen path ownership: packaged resources are immutable inputs, while every mutable artifact belongs under a per-user local application-data root. Source mode intentionally preserves repository-local paths for developer ergonomics; packaged smoke must override `LOCALAPPDATA` and prove the distribution tree stays unchanged.
41. Ephemeral bounded conversation: follow-up questions may use only recent complete successful exchanges, capped independently by count and characters. Conversation state belongs to the current Chat page, is never written to SQLite, and cannot turn a refused or degraded response into future prompt context.
42. Event-loop-owned shutdown: smoke and interactive close both enter the real window close path. Application exit occurs only after every page/reminder worker has finished and the process-owned review lease has been released, including when the window is already hidden during deferred close.
43. Lost-finish recovery: a close watcher must connect to `finished` before checking the worker state again. The signal covers future completion and the post-connection state check covers completion that raced ahead of the connection; duplicate deferred-close scheduling is harmless and preferable to a hidden hung process.
44. Absolute reminder wake-up: persisted cooldown state owns the target time and the UI owns only timer scheduling. Both heartbeat and one-shot paths re-evaluate through the same atomic policy; no UI timer is allowed to bypass the database claim or independently decide to notify.
45. Layered notification delivery: deterministic ReminderService state decides whether a reminder exists; the operating-system adapter decides only how to present it. Native callbacks must enter Qt through a queued signal, and any native backend failure falls back to the tray without changing notification claims or scheduling.
46. Notification identity ownership: only a frozen executable carrying the installer-only marker and matching Start shortcut may claim the dedicated AUMID. All source, portable, and installed processes remove their own tracked CET-Agent Toasts individually and never clear a whole identity, preserving reminders owned by concurrent instances.
47. Current-user installer ownership: the installer copies only immutable program files under the user's Programs directory. Mutable learning state remains in the separate application-data root and survives upgrades/uninstall. Stable AppId/AUMID values are release compatibility contracts even while icon, version-resource, signature, and remote-delivery work is deferred.
48. Deterministic recognition quiz: the correct meaning and all distractors come from validated local vocabulary, with stable ranking/shuffling and a safe active-recall fallback. The LLM may explain a card but never creates or grades its choices.
49. Voluntary staged continuation: extra learning is an explicit five-card transaction over untouched future cards for one selected level. Already-released untouched work blocks skipping ahead, reviewed cards are immutable, and completing the current new-word pack is required before requesting another; learned-due work remains independent in its own route.
50. Snapshot contextual assistance: the embedded assistant captures only the current ReviewItem when Send is pressed, labels that word in the transcript, bounds context in the Prompt layer, and shares the existing Provider/routing rules without sharing a worker or history with the full assistant page. Study pages explicitly announce each new displayed card, immediately clearing transcript/history and advancing a generation token; a response may render or enter history only if its captured generation and current context still match. Same-card follow-ups remain conversational.
51. Synthetic evidence isolation: `demo_confusion` ReviewLogs may drive the repeatable analysis demo, but they are not user reviews and must never alter LearningState aggregates or FSRS card state. Schema v5 repairs already polluted databases without deleting graph evidence.
52. Deterministic contextual memory help: ordinary embedded memory requests never call the 3B model. The application owns core meaning, bounded stored collocations/family links, complete example masking, and meaning-to-English recall, including explicit missing-content fallbacks; it never invents roots, etymology, homophones, or letter stories. Only an explicit advanced-Provider choice may use a model for deeper help, and that output remains grounded to the captured card.
53. Exact latest-review undo: undo is a compensating database transaction, not a UI-only queue edit. Each new review persists the missing pre-review schedule snapshot; only a card whose current `last_review_at` still matches that log may be restored, counters and FSRS fields move together, and legacy logs remain intentionally non-undoable.
54. Deterministic personal wordbook: favorites are explicit user state keyed once per Word, not an LLM classification and not an FSRS signal. The collection spans study levels, shows the stored level, and may be changed only by user action.
55. Gated generated learning aids: the review card owns bounded display fields for collocations and word families, and the service must preserve aid-row presence so a legitimate empty list cannot be confused with missing content. Model text must cross a separately tested schema, identity-aware cache, and word-alignment validator before the application may persist or present it as study content.
56. Separate generated-content layer: word learning aids live in a dedicated JSONL artifact with provenance and a one-to-one `word_learning_aids` table, never in `words` or FSRS state. Generation input is limited to per-batch CSV fields, promotion requires a complete offline validation pass, import is idempotent and content-hash-aware, and all material stays `ai_generated_unreviewed` until a real review process exists.
57. Offline generation budget separation: interactive application Providers retain the 2,048-token response ceiling. Only the explicitly invoked DeepSeek vocabulary generator enables JSON Output, disables thinking, and requests a bounded 8,192-token response; a length finish is rejected instead of parsing partial JSON.
58. Generated-artifact defense in depth: the generator validates before checkpoint/promotion, the standalone complete validator verifies content plus provenance hashes/statistics/model identity, and bootstrap repeats the same source/provenance gate before any database transaction. A valid Pydantic shape alone is never sufficient for persistence.
59. Generation repair feedback and safe sanitization: validation failures feed concrete local errors into bounded retries; deterministic cleanup removes duplicate/self/obvious-inflection family noise and invalid collocations only when the schema minimum remains satisfied. This improves completion without allowing unvalidated content into the artifact.
60. Readable learning-aid lists: the review card preserves each formatted collocation or word-family entry on its own line, avoiding a dense middle-dot run when several items are present.
61. Feedback is an overlay, not a rewrite: a user's latest bounded issue category is stored separately from the generated artifact and scheduling data. Reporting can inform later human review, but it cannot silently mutate displayed source material, mark an item reviewed, or affect FSRS.
62. Native bounded overflow: only the collocation/word-family content groups enter a capped `QScrollArea`; Qt owns mouse-wheel behavior and shows the vertical scrollbar only when needed. Trust status and feedback controls remain outside that viewport so overflow cannot hide the content-quality action.
63. Grounded contextual distinction: the review quick action asks for current-word usage boundaries rather than asking the model to discover personal confusion pairs. Its deterministic route recognizes distinction vocabulary, while the Prompt layer permits concrete comparison words only when the captured card context supplies them; model recall alone is never evidence of user confusion.
64. Explicit learning intent: untouched acquisition and learned-card review are disjoint deterministic queries over LearningState/history, not UI labels over one mixed due queue. New cards show validated content before recall testing; learned cards appear only when their FSRS time is due.
65. Non-scheduling optional practice: early/manual recall is useful evidence but is not an FSRS review. It receives a separate `PracticeLog`, scope, correctness and response time, while LearningState, ReviewLog, reminders, dashboard FSRS accuracy, and the confusion graph remain unchanged.
66. Clock-anomaly transparency: future user ReviewLogs are never silently trusted or silently repaired. Current-time selectors exclude them, and the dashboard reports their bounded count/latest time so the user can correct the operating-system clock before schedules naturally catch up.
67. Acquisition before scheduling: proficiency 0–3 is a durable learning state separate from FSRS. Acquisition attempts may advance only through the deterministic task assigned to the current level and never create a synthetic ReviewLog or inferred FSRS rating; graduation schedules the first real review exactly 24 hours later.
68. Round-robin acquisition ownership: the application loads a bounded group, returns each unfinished word to that same group's tail, and persists every transition so restart resumes from the stored proficiency rather than replaying only UI memory. Incorrect answers remain useful attempt evidence but cannot advance proficiency.
69. User-owned mastery exclusion: “完全掌握” is an explicit, reversible user decision represented separately from LearningState. Exclusion is applied consistently to every source of active study work and reminder/analysis candidates, while unmarking restores the preserved prior path instead of resetting or rescheduling the word.
70. Answer-isolation by stage: each acquisition stage exposes only the information required for its recall direction. UI convenience, generated aids, phonetics, and contextual AI must not reveal an answer before the deterministic attempt boundary records it.
71. Complete cloze masking: when an example contains multiple occurrences or inflected forms of the target, every target-form occurrence is masked before presentation; the first actual surface form determines the correct option morphology. The offline validator covers all 4,611 bundled records without a model call.
72. Explicit acquisition group boundary: after the current round-robin group is exhausted, the UI checks released work asynchronously and shows a deliberate “开始下一组新词” action; only when no released work remains does the same completion control invoke the existing five-word unlock transaction.
73. Visible card conversation boundary: clearing only model history is insufficient because an old transcript and a late worker result both look attached to the new word. Card transitions therefore clear presentation and history together, cancel idle routing state, and invalidate in-flight presentation by generation without attempting unsafe QThread termination.
74. Accessible chat-role presentation: color reinforces but never replaces the explicit speaker/status label. Every rich-text field crosses HTML escaping first, and the UI presents answer ownership without exposing an internal model/routing confidence score. Deterministic output is identified as a local rule result rather than model output.
75. Visible answer-model ownership: automatic routing remains the cost-safe default, but the learner can force local or advanced dispatch without manipulating prompt wording. The selected mode is locked with the pending request, advanced progress/metadata are explicit, unavailable configuration is visible but not selectable, and changing cards does not silently change the learner's model preference.
76. Bounded external lexical overlay: current-card context defines the target word and sense. Reproducible OEWN/COW/ECDICT candidates may be shown from the local pending overlay; only an explicit advanced request may add general model-recalled synonym/antonym knowledge. The Prompt limits common candidates and requires differences/examples; the Service adds an unavoidable unreviewed-content label; no generated candidate is persisted, treated as validated aid data, or attributed to personal confusion evidence.
77. Rejected local answer is replacement input, not history: the one-click advanced retry is explicit user consent for the same bounded task. It restores the snapshot taken before the local request and sends the same question/card context, so the higher-quality answer is not biased by text the learner already rejected. A new question or card transition destroys that retry capability; a retry never changes the panel's persistent selector or writes either answer to durable storage.
78. Adaptive lexical-card composition: `词形 / 搭配 / 近反义 / 派生词` is a set of optional verified or explicitly pending sections, not a required four-cell layout. Section presence, order, size, trust label, and responsive wrapping come from a deterministic projection; absent or low-value content leaves no empty learner-facing heading. Proactive high-value facts should remove the need for routine LLM questions without filling the card with irrelevant lexical noise.
79. Typed paradigms are not word families: noun number/countability, verb tense/participle, adjective/adverb degree, numeral cardinal/ordinal, pronoun case, pronunciation variants, and regional alternatives require explicit structured form types and sense labels. Derivation remains a separate relationship. Irregular or answer-critical forms are never inferred from string suffixes at presentation time or supplied from model recall.
80. Verified lexical facts precede explanation: reproducible licensed sources and audited deterministic exceptions own morphology and sense-bound relation candidates. The UI and factual query handler consume the same projection; a recognized factual question returns verified data or a clearly pending source candidate without a Provider call when available, otherwise reports that verified data is missing. LLMs may explain or supplement only under an explicit unreviewed boundary and can never overwrite, promote, or persist their recollection as a lexical fact.
81. Lexical-fact artifact isolation: typed paradigms and formally verified grouped relations live in `word_lexical_facts.jsonl` plus schema-v13 `word_lexical_facts`, never in the generated-aid artifact, `words`, or FSRS state. Full-artifact validation precedes any formal write; runtime rows are hash-aware, while candidate relations use separate hash-aware columns.
82. Deterministic factual interception: `LexicalFactQuery` runs before Provider dispatch, resolves both headwords and known surface forms, returns `deterministic-lexical-fact` for formal values or `deterministic-lexical-candidate` for the isolated pending overlay, and reports missing verified data locally. The ChatPanel preflight avoids an unnecessary advanced confirmation when the local index has a value. Advanced supplementation remains explicit and unreviewed; qwen2.5:3b cannot be a morphology correctness fallback.
83. Adaptive card composition: the same service projection controls section order, visibility, trust status, assistant context eligibility, and native overflow. A section's absence is hidden in learner-facing UI while missing versus verified-empty status remains available to validators/diagnostics.
84. Source-rights-first lexical expansion: phase-2 card quality should come primarily from reproducibly extracted dictionary evidence, with exact-version ECDICT plus verified public-domain, CC0/waived, or permissively redistributable resources preferred over model recall and restricted commercial dictionaries. “Open source” and “old dictionary” are not license conclusions: every ingested field retains a pinned source, hash, transformation, and applicable notice; uncertain rights or provenance means exclusion, while missing content stays hidden. LLMs may normalize or explain a source-fixed candidate only under the existing unreviewed boundary.
85. Candidate audit before promotion: source approval and source accuracy are separate gates. Exact hashes and compatible rights permit a source to enter the offline audit, but no value becomes `source_validated` until field-level evidence, POS/sense alignment, deterministic conflict handling, and the relevant human pilot pass. ECDICT exchange conflicts are classified rather than blindly preferred, OEWN and COW join only through shared ILI, and candidate metrics may enter only the isolated schema-v13 candidate overlay with an explicit pending-review label; they never overwrite formal facts or scheduling state.
86. Field-level form evidence stays candidate-only: `word_lexical_fact_candidates.jsonl` must cover the exact vocabulary set and preserve the current/source role values plus ECDICT version, exchange code locator, source hash, manifest hash, and canonical record hash. Heuristic labels such as legacy-rule candidate or possible regional spelling are review ordering hints only; no script may overwrite `word_lexical_facts.jsonl`, SQLite, or learner-facing cards from this artifact without a separately recorded human audit and promotion gate.
87. Relation candidate overlay balances coverage and trust: WordNet/COW groups must retain Chinese-overlap alignment, single-word ECDICT targets (including outside-bank reference words), positive frequency, compatible POS, an ordinary learner-facing translation, at most six targets per relation type, and at most four groups per headword. Multi-sense and no-aligned records remain explicit statuses; the v3 overlay's 2,383 synonym-bearing words are displayed only as `candidate_only`, specialist-only translations are hidden, and no candidate relation is promoted to `source_validated` without source/evidence and human review.
88. Candidate visibility without trust collapse: schema-v13 stores candidate relations separately from formal relations. The card and deterministic assistant may show them as `来源候选 · 待审核` while each relation item remains target-focused (`近义/反义：目标词 adj./adv./n./v. 翻译`), but candidate targets never create vocabulary rows, learning states, queues, reminders, review logs, confusion edges, or formal lexical hashes. Formal facts take precedence when both layers contain the same target.
89. Read-only linked-word detail: clickable relation/derivative targets open a modal, scrollable word card through a service/controller boundary. In-bank targets reuse the same deterministic lexical projection and generated-aid display; outside-bank targets remain reference-only with source-candidate/AI trust text. The popup performs no database writes, scheduling, LLM calls, or external navigation, and stale asynchronous results are ignored by generation token.
90. Deferred editorial/trust work: manual semantic review of AI-generated content, candidate promotion, new editorial feedback categories, and trust-label redesign are outside the current implementation sequence. Existing content statuses and learner-facing labels remain intact; deferral never authorizes promotion without the documented human/source gate. Near-term development follows the non-editorial optimization backlog in section 6.
91. Confusion-cluster practice: the analysis page may launch an explicit, bounded queue from the selected cluster's deterministic word IDs. The queue is still optional practice: each answer writes only a `PracticeLog` tagged with the cluster scope, while FSRS/`LearningState`, formal `ReviewLog`, reminders, and confusion-graph evidence remain untouched. Mastered, unlearned, out-of-level, and duplicate IDs are filtered at the service boundary.
92. Global lookup remains read-only: exact/prefix search is bounded and local, empty/invalid queries return no full-bank dump, and every list-page detail action constructs a `LinkedWordReference` for the existing controller. Search results, favorites, mastered words, dashboard wrong words, and confusion-cluster words never create a `Word`, queue item, review log, or practice obligation.
93. Linked-detail worker cleanup: a finished `WordDetailController` worker is removed from its tracking set, detached from the controller QObject parent, and scheduled with `deleteLater()` before late-generation results can be considered. This prevents a parent QObject garbage-collection race from destroying a completed QThread while a subsequent page worker starts; the main-window deferred-close path still tracks only running workers.
94. Dashboard/session summaries are projections: route buttons dispatch only to the existing learning/review pages, while acquisition/review/practice counters are accumulated from already-persisted submission results. They may explain attempts, mistakes, graduation, recall, and next-due timing, but never create scheduling events, infer hidden history, or mutate service state.
95. Linked-detail context is non-authoritative UI metadata: lexical projections attach the source headword, source meaning, and relation label to each target reference; the dialog may compare those values with the loaded target and restore keyboard focus, but trust status, card facts, persistence, and navigation eligibility still come from the existing deterministic service/controller boundary.
96. Workload settings are forward-safe: daily release is computed in deterministic frequency order only when a CET level is first activated, while group size and extra-study pack size are read by new sessions. Defaults remain 20/10/5, values are bounded at configuration and service boundaries, and no setting path moves reviewed cards, changes another level, or rewrites an existing activation.
97. UI decomposition follows ownership boundaries: page classes retain Qt widgets, asynchronous workers, and service callbacks; shared session enums/labels and completion text are dependency-light modules with pure tests. No generic controller abstraction is introduced merely to reduce line count.
98. Lexical-fact hygiene is migration-only and projection-aware: v14 deletes a row only when both formal JSON layers are empty and both statuses are `missing`, while requiring the parent `words` table and all candidate columns. Candidate-only or formal rows survive, the transaction rolls back on failure, and the compact importer remains the source of truth for future starts.
99. Distribution freshness is verified from source: every material release rebuilds the PyInstaller onedir tree before compiling the unsigned installer, then launches both the onedir and installed executables with isolated `LOCALAPPDATA`. Smoke must prove writable schema-v14 initialization and database integrity without touching developer data; ignored build outputs are evidence only and are never committed as application state.

## 8. Development constraints

- Preserve the four-layer boundary and existing useful code.
- Do not introduce Electron, React, Vue, PostgreSQL, Redis, Docker microservices, Kubernetes, Celery, message queues, AutoGen, CrewAI, or a complex multi-agent framework for this MVP.
- Do not add login, social features, rankings, cloud sync, essays, listening, reading, OCR, speech recognition, or gamification unless the user explicitly expands scope.
- Do not let the LLM read the whole database or calculate statistics.
- Keep all prompts in `app/ai/prompts.py`.
- Keep secrets only in local `.env`; never put them in code, tests, logs, README, or this file.
- Use type hints, small single-purpose functions, PEP 8 naming, dataclasses/Pydantic where appropriate, and comments only where formulas or rationale need explanation.
- Aim to keep Python modules below roughly 300–400 lines; split responsibilities before a file becomes oversized.
- UI operations that can block must run off the GUI thread.
- Database updates that belong to one user action must share a transaction.
- New deterministic behavior requires deterministic tests.
- Do not use prompt wording, local-model confidence, or an advanced-model retry as the correctness boundary for morphology, grammatical paradigms, ordinals, or sense-bound lexical relations.
- Do not render empty lexical-card sections, and do not conflate grammatical forms with `word_family` derivatives.
- Prefer the smallest reliable implementation that fits the existing architecture.

## 9. Required agent workflow

For every implementation milestone:

1. Read this file, then inspect the related code and tests before editing.
2. Check for existing behavior and avoid duplicate implementations.
3. State the intended boundary and smallest coherent change.
4. Implement with small patches and preserve unrelated workspace changes.
5. Run focused tests, then the full suite when the milestone is complete.
6. Run offscreen smoke for startup/UI lifecycle changes.
7. Record real verification evidence in section 5.
8. Update sections 4–7 whenever behavior, architecture, decisions, or backlog changes.
9. Do not leave a completed task described as pending, and do not claim an unexecuted check passed.

When context is compacted or work is interrupted, this file—not conversational memory—must be enough to reconstruct the active project state and next safe action.
